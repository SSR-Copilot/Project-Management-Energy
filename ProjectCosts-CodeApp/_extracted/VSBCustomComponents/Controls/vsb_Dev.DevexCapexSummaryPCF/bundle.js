/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
var pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad;
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./TableConnectedToDataversePCF/index.ts"
/*!***********************************************!*\
  !*** ./TableConnectedToDataversePCF/index.ts ***!
  \***********************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DevexCapexSummaryPCF: () => (/* binding */ DevexCapexSummaryPCF)\n/* harmony export */ });\n/* harmony import */ var _ui_ActionDropdown__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ui/ActionDropdown */ \"./TableConnectedToDataversePCF/ui/ActionDropdown.ts\");\n/* harmony import */ var _ui_GridRenderer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./ui/GridRenderer */ \"./TableConnectedToDataversePCF/ui/GridRenderer.ts\");\n\n\n/**\n * DevexCapexSummaryPCF\n *\n * Main entry point for the PCF Control.\n * Acts as the orchestrator between the Power Apps framework and our custom UI components.\n *\n * Deliberately thin: it owns framework plumbing (lifecycle, outputs, the\n * notifyOutputChanged contract) and nothing visual, so UI churn in\n * GridRenderer/ActionDropdown never risks breaking the Canvas contract.\n */\nclass DevexCapexSummaryPCF {\n  /**\n   * Handler body for the PCF_RE_RENDER custom window event. GridRenderer\n   * dispatches this event when a user expands/collapses a row: the renderer\n   * cannot safely rebuild the DOM from inside its own click handler (it\n   * would be destroying the very elements whose event is still bubbling),\n   * so it asks the root control to perform the re-render instead.\n   */\n  constructor() {\n    /**\n     * Output trigger booleans + selection outputs (mirror of IOutputs).\n     * These are transient \"pulse\" values, not persistent state: triggerAction\n     * sets exactly one boolean true, getOutputs hands the snapshot to Canvas\n     * and immediately resets everything to defaults. They exist as fields only\n     * because the framework reads outputs asynchronously (notifyOutputChanged\n     * now, getOutputs later), so the action must be parked somewhere between\n     * those two calls.\n     */\n    // Output Trigger Action Properties\n    this._editTriggered = false;\n    this._commentTriggered = false;\n    this._deleteTriggered = false;\n    this._addTriggered = false;\n    this._addStandardContractTriggered = false;\n    this._setCostPaidUnpaidTriggered = false;\n    this._selectedRecordId = \"\";\n    this._selectedStandardContractId = \"\";\n    this._selectedCostContractId = \"\";\n    this._selectedCostYear = 0;\n    this._selectedCostMonth = 0;\n    this._selectedCostPaid = false;\n    /**\n     * UI state owned HERE (not in GridRenderer) on purpose: every updateView\n     * triggers a full re-render, so expand/collapse state must outlive each\n     * render pass. The Set is passed by reference into GridRenderer, which\n     * mutates it; keeping ownership at the root means the state also survives\n     * if GridRenderer were ever re-created.\n     */\n    // Global state. Inverted semantics (Bug 13472): this set tracks EXPANDED\n    // rows, so an empty set = everything collapsed - the required default on\n    // screen entry / tab change. Cleared whenever CollapseResetKey changes.\n    this._expandedRowIds = new Set();\n    // Last CollapseResetKey seen; when Canvas changes it (tab switch / screen\n    // OnVisible) the expand state is wiped so the grid re-collapses.\n    this._lastResetKey = \"\";\n    // Store bound reference for event listener to properly add/remove it\n    this._onReRenderEvent = () => {\n      // Guard: the event can theoretically fire before init/updateView\n      // have run (or after destroy) — rendering then would crash on\n      // undefined members, so silently skip until the control is ready.\n      if (this._context && this._gridRenderer) {\n        this._gridRenderer.render(this._context);\n      }\n    };\n  }\n  /**\n   * Used to initialize the control instance. Controls can kick off remote server calls and other initialization actions here.\n   * Data-set values are not initialized here, use updateView.\n   * @param context The entire property bag available to control via Context Object; It contains values as set up by the customizer mapped to property names defined in the manifest, as well as utility functions.\n   * @param notifyOutputChanged A callback method to alert the framework that the control has new outputs ready to be retrieved asynchronously.\n   * @param state A piece of data that persists in one session for a single user. Can be set at any point in a controls life cycle by calling 'setControlState' in the Mode interface.\n   * @param container If a control is marked control-type='standard', it will receive an empty div element within which it can render its content.\n   */\n  init(context, notifyOutputChanged, state, container) {\n    this._context = context;\n    this._notifyOutputChanged = notifyOutputChanged;\n    context.mode.trackContainerResize(true);\n    // Initialize child components following Separation of Concerns\n    // Both UI components receive triggerAction as their callback so EVERY\n    // user action — whether it originates from the row dropdown\n    // (Edit/Comment/Delete) or from the grid itself (Add buttons, paid\n    // toggles) — funnels through one choke point. That single funnel is\n    // what guarantees the \"exactly one trigger boolean true\" contract\n    // Canvas OnChange relies on; .bind(this) preserves the control's\n    // `this` when the callback fires from deep inside DOM handlers.\n    this._actionDropdown = new _ui_ActionDropdown__WEBPACK_IMPORTED_MODULE_0__.ActionDropdown(this.triggerAction.bind(this));\n    this._gridRenderer = new _ui_GridRenderer__WEBPACK_IMPORTED_MODULE_1__.GridRenderer(container, this._expandedRowIds, this._actionDropdown, this.triggerAction.bind(this));\n    // Listen for internal re-render requests from UI components (like toggle collapse)\n    // A custom window event is used (instead of GridRenderer calling\n    // render() on itself) so the re-render happens outside the click\n    // handler's call stack — see the constructor note on _onReRenderEvent.\n    window.addEventListener(\"PCF_RE_RENDER\", this._onReRenderEvent);\n  }\n  /**\n   * Called when any value in the property bag has changed. This includes field values, data-sets, global values such as container height and width, offline status, control metadata values such as label, visible, etc.\n   * @param context The entire property bag available to control via Context Object; It contains values as set up by the customizer mapped to names defined in the manifest, as well as utility functions\n   */\n  updateView(context) {\n    var _a, _b;\n    this._context = context;\n    // Bug 13472: collapse all rows whenever Canvas signals a tab switch or\n    // screen re-entry via CollapseResetKey. Clearing the expanded set leaves\n    // every account/subaccount collapsed. A plain data refresh (paid toggle,\n    // filter change) keeps the same key, so the user's expansions survive.\n    var resetKey = (_b = (_a = context.parameters.CollapseResetKey) === null || _a === void 0 ? void 0 : _a.raw) !== null && _b !== void 0 ? _b : \"\";\n    if (resetKey !== this._lastResetKey) {\n      this._expandedRowIds.clear();\n      this._lastResetKey = resetKey;\n    }\n    // A canvas-app PCF dataset delivers only the FIRST page by default, so a\n    // large bound collection is truncated and the tail rows go missing (e.g.\n    // the last account never renders until the row count shrinks). Pull every\n    // remaining page: loadNextPage() re-fires updateView with the accumulated\n    // records, so the grid fills in progressively until no page is left.\n    var summaryDataset = context.parameters.DevexCapexCostSummary;\n    if (summaryDataset && !summaryDataset.loading && summaryDataset.paging.hasNextPage) {\n      summaryDataset.paging.loadNextPage();\n    }\n    var optionsDataset = context.parameters.StandardContractOptions;\n    if (optionsDataset && !optionsDataset.loading && optionsDataset.paging.hasNextPage) {\n      optionsDataset.paging.loadNextPage();\n    }\n    // Delegate rendering to the GridRenderer\n    this._gridRenderer.render(this._context);\n  }\n  /**\n   * Centralized action handler to process outputs triggered by UI components.\n   *\n   * This is the ONLY place output state may be written. Every assignment\n   * below uses `action === \"...\"` comparisons (rather than setting just the\n   * matching flag) so each call atomically sets exactly one trigger true AND\n   * clears the other five — Canvas OnChange branches on \"which boolean is\n   * true\", and two simultaneous trues would fire two business operations.\n   * Unrelated selection outputs are likewise overwritten (not left over from\n   * the previous action) so Canvas never reads stale IDs.\n   * @param action The specific action triggered\n   * @param recordId The associated record ID for the action\n   */\n  triggerAction(action, recordId) {\n    var standardContractId = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : \"\";\n    var costPaidPayload = arguments.length > 3 ? arguments[3] : undefined;\n    this._editTriggered = action === \"Edit\";\n    this._commentTriggered = action === \"Comment\";\n    this._deleteTriggered = action === \"Delete\";\n    this._addTriggered = action === \"Add\";\n    this._addStandardContractTriggered = action === \"AddStandardContract\";\n    this._setCostPaidUnpaidTriggered = action === \"SetCostPaidUnpaid\";\n    this._selectedRecordId = recordId;\n    this._selectedStandardContractId = standardContractId;\n    // SelectedCostContractId is a dedicated output for the paid/unpaid\n    // flow only: Canvas reads it (instead of SelectedRecordId) so its\n    // cost-cell formula can't accidentally act on an ID left behind by an\n    // Edit/Delete action; for all other actions it is deliberately blank.\n    this._selectedCostContractId = action === \"SetCostPaidUnpaid\" ? recordId : \"\";\n    this._selectedCostYear = (costPaidPayload === null || costPaidPayload === void 0 ? void 0 : costPaidPayload.year) || 0;\n    this._selectedCostMonth = (costPaidPayload === null || costPaidPayload === void 0 ? void 0 : costPaidPayload.month) || 0;\n    this._selectedCostPaid = (costPaidPayload === null || costPaidPayload === void 0 ? void 0 : costPaidPayload.paid) || false;\n    // Notify host of state change\n    // This is what makes the framework call getOutputs() and, in turn,\n    // fire the Canvas App's OnChange — without it the action would sit\n    // unread in the private fields forever.\n    this._notifyOutputChanged();\n  }\n  /**\n   * It is called by the framework prior to a control receiving new data.\n   *\n   * TRIGGER RESET PATTERN (do not remove): after snapshotting the outputs,\n   * every trigger/selection field is reset to its default. Canvas OnChange\n   * only fires when an output VALUE CHANGES — if EditTriggered stayed true,\n   * a second Edit click on the same row would produce true -> true (no\n   * change) and Canvas would silently ignore the user's action. Resetting\n   * here guarantees each action yields a fresh false -> true transition.\n   * The side effect (Canvas also sees a true -> false \"reset\" change) is\n   * expected; the Canvas formula treats the all-false state as a no-op.\n   * @returns an object based on nomenclature defined in manifest, expecting object[s] for property marked as \"bound\" or \"output\"\n   */\n  getOutputs() {\n    var outputs = {\n      EditTriggered: this._editTriggered,\n      CommentTriggered: this._commentTriggered,\n      DeleteTriggered: this._deleteTriggered,\n      AddTriggered: this._addTriggered,\n      AddStandardContractTriggered: this._addStandardContractTriggered,\n      SetCostPaidUnpaidTriggered: this._setCostPaidUnpaidTriggered,\n      SelectedRecordId: this._selectedRecordId,\n      SelectedStandardContractId: this._selectedStandardContractId,\n      SelectedCostContractId: this._selectedCostContractId,\n      SelectedCostYear: this._selectedCostYear,\n      SelectedCostMonth: this._selectedCostMonth,\n      SelectedCostPaid: this._selectedCostPaid\n    };\n    // Reset AFTER capturing the snapshot above: the framework gets the\n    // \"pulsed\" values while the internal state returns to idle, arming\n    // the control for the next action (see JSDoc — Canvas change\n    // detection depends on this).\n    this._editTriggered = false;\n    this._commentTriggered = false;\n    this._deleteTriggered = false;\n    this._addTriggered = false;\n    this._addStandardContractTriggered = false;\n    this._setCostPaidUnpaidTriggered = false;\n    this._selectedRecordId = \"\";\n    this._selectedStandardContractId = \"\";\n    this._selectedCostContractId = \"\";\n    this._selectedCostYear = 0;\n    this._selectedCostMonth = 0;\n    this._selectedCostPaid = false;\n    return outputs;\n  }\n  /**\n   * Called when the control is to be removed from the DOM tree. Controls should use this call for cleanup.\n   * i.e. cancelling any pending remote calls, removing listeners, etc.\n   */\n  destroy() {\n    // Delegate cleanup to child components\n    if (this._actionDropdown) {\n      this._actionDropdown.destroy();\n    }\n    if (this._gridRenderer) {\n      this._gridRenderer.destroy();\n    }\n    // Must be removed explicitly: the listener lives on the global window\n    // (not on the control's container), so the framework cannot clean it\n    // up for us — leaving it would leak this control instance and cause\n    // ghost re-renders after navigation back to the screen.\n    window.removeEventListener(\"PCF_RE_RENDER\", this._onReRenderEvent);\n  }\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./TableConnectedToDataversePCF/index.ts?\n}");

/***/ },

/***/ "./TableConnectedToDataversePCF/services/DatasetParser.ts"
/*!****************************************************************!*\
  !*** ./TableConnectedToDataversePCF/services/DatasetParser.ts ***!
  \****************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DatasetParser: () => (/* binding */ DatasetParser)\n/* harmony export */ });\nfunction _toArray(r) { return _arrayWithHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableRest(); }\nfunction _nonIterableRest() { throw new TypeError(\"Invalid attempt to destructure non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.\"); }\nfunction _unsupportedIterableToArray(r, a) { if (r) { if (\"string\" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return \"Object\" === t && r.constructor && (t = r.constructor.name), \"Map\" === t || \"Set\" === t ? Array.from(r) : \"Arguments\" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }\nfunction _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }\nfunction _iterableToArray(r) { if (\"undefined\" != typeof Symbol && null != r[Symbol.iterator] || null != r[\"@@iterator\"]) return Array.from(r); }\nfunction _arrayWithHoles(r) { if (Array.isArray(r)) return r; }\n/**\n * Service class responsible for data transformation.\n * Parses the flat Dataverse/Canvas App dataset into a nested tree structure.\n *\n * Stateless (all static): every call re-derives the field mapping from the live\n * dataset columns, so Canvas can rename/alias columns without a PCF rebuild.\n */\nclass DatasetParser {\n  /**\n   * Parses the flat sampleDataSet records and maps them to a hierarchical tree.\n   *\n   * Two-pass algorithm: pass 1 buckets every record into accounts/subaccounts/\n   * contracts keyed by normalized RowId; pass 2 stitches children onto parents\n   * via ParentId. Child order inside each parent follows dataset.sortedRecordIds\n   * (i.e. the Canvas collection order) — sorting is a Canvas responsibility.\n   * Orphans (ParentId with no matching RowId) are dropped without warning.\n   */\n  static parseDatasetToTree(dataset) {\n    var columns = dataset.columns;\n    // Dynamically find field names in a case-insensitive manner\n    // Canonical defaults double as last-resort field names if no column matches.\n    // Empty-string defaults (costPaidByField, isStandardContractField, ...) mean\n    // \"not detected\" — firstAvailableValue()/safeGetValue() skip empty names, so\n    // the hard-coded per-record alias lists further down still get a chance.\n    var rowIdField = \"RowId\";\n    var parentIdField = \"ParentId\";\n    var typeField = \"Type\";\n    var numberField = \"Number\";\n    var nameField = \"Name\";\n    var subLabelField = \"SubLabel\";\n    var costPaidByField = \"\";\n    var totalCostField = \"TotalCost\";\n    var plannedCostField = \"PlannedCost\";\n    var actualCostField = \"ActualCost\";\n    var isAccountField = \"\";\n    var isContractField = \"\";\n    var isStandardContractField = \"\";\n    var standardContractOptionsField = \"\";\n    var distributionTypeField = \"\";\n    var commentTooltipField = \"\";\n    // Index 0..11 = Jan..Dec of the SELECTED year. mFields pre-seeds \"M1\"..\"M12\";\n    // the paid/comment arrays start empty and are only filled when a real column\n    // is detected (empty = rely on per-record alias probing instead).\n    var mFields = Array(12).fill(\"\").map((_, i) => \"M\".concat(i + 1));\n    var monthPaidFields = Array(12).fill(\"\");\n    var monthHasCommentFields = Array(12).fill(\"\");\n    var monthCommentTooltipFields = Array(12).fill(\"\");\n    // Single pass over dataset columns: resolve each logical field to the column\n    // name actually present in THIS dataset. normalizeFieldName strips casing and\n    // punctuation so \"vsb_IsStandardContract\", \"Is Standard Contract\" and\n    // \"isstandardcontract\" all collapse to the same alias key.\n    columns.forEach(col => {\n      var lowerName = col.name.toLowerCase();\n      var normalizedName = this.normalizeFieldName(col.name);\n      // RowId aliases include the Dataverse primary keys of BOTH source tables\n      // (capexaccountlist for accounts/subaccounts, capexprojectcontract for\n      // contracts) because row types originate from different entities.\n      if ([\"rowid\", \"id\", \"vsbcapexaccountlistid\", \"capexaccountlist\", \"capexprojectcontract\"].includes(normalizedName)) rowIdField = col.name;else if ([\"parentid\", \"vsbparentaccountid\", \"parentaccount\"].includes(normalizedName)) parentIdField = col.name;else if (normalizedName === \"type\") typeField = col.name;else if (normalizedName === \"number\" || normalizedName === \"vsbnumber\") numberField = col.name;else if ([\"name\", \"vsbname\", \"vsbcostdescription\", \"costdescription\"].includes(normalizedName)) nameField = col.name;else if (normalizedName === \"sublabel\") subLabelField = col.name;else if ([\"costpaidby\", \"costpaidbyfilter\", \"vsbcostpaidby\", \"costtype\", \"vsbcosttype\"].includes(normalizedName)) costPaidByField = col.name;else if ([\"totalcost\", \"totalcosts\", \"vsbtotalcost\", \"totalcostamount\"].includes(normalizedName)) totalCostField = col.name;else if ([\"plannedcost\", \"totalplannedcost\", \"totalplannedcosts\"].includes(normalizedName)) plannedCostField = col.name;else if ([\"actualcost\", \"totalactualcost\", \"totalactualcosts\"].includes(normalizedName)) actualCostField = col.name;else if (normalizedName === \"isaccount\") isAccountField = col.name;else if (normalizedName === \"iscontract\") isContractField = col.name;\n      // Standard-contract flag arrives under many names depending on whether a\n      // Canvas collection column, a Dataverse logical name (vsb_*) or a display\n      // name is bound. Removing ANY alias silently breaks standard-contract\n      // detection for the data shape that uses it.\n      else if ([\"isstandardcontract\", \"standardcontract\", \"isstandard\", \"isstandardcost\", \"standardcost\", \"contracttype\", \"standardcontracttype\", \"vsbisstandardcontract\", \"vsbisstandardcost\", \"vsb_isstandardcontract\", \"vsb_isstandardcost\"].includes(normalizedName)) isStandardContractField = col.name;else if ([\"standardcontractoptions\", \"standardcontracts\", \"availablestandardcontracts\"].includes(normalizedName)) standardContractOptionsField = col.name;\n      // DistributionType gates the month paid/unpaid action in the grid, so its\n      // alias list must cover every spelling Canvas/Dataverse can emit.\n      else if ([\"distributiontype\", \"distribution\", \"costdistributiontype\", \"contractdistributiontype\", \"vsbdistributiontype\", \"vsbcostdistributiontype\", \"vsb_distributiontype\"].includes(normalizedName)) distributionTypeField = col.name;\n      // Contract-level comment tooltip: long alias list because this column was\n      // renamed several times in the Canvas collection over the project history.\n      else if ([\"commenttooltip\", \"generalcommenttooltip\", \"contractcommenttooltip\", \"commenttext\", \"commentdescription\", \"generalcomment\", \"contractcomment\", \"latestcomment\", \"commentsummary\"].includes(normalizedName)) commentTooltipField = col.name;\n      // Month-indexed columns: cost value (M{i}/Month{i}), paid flag (8 alias\n      // spellings: M{i}Paid, Month{i}Paid, M{i}IsPaid, PaidM{i}, CostPaidM{i}, ...),\n      // month-level comment flag, and month-level comment tooltip. These alias\n      // families must stay in sync with whatever the Canvas AddColumns() emits;\n      // resolving them once here keeps the hot per-record loop cheap.\n      for (var i = 1; i <= 12; i++) {\n        if (normalizedName === \"m\".concat(i) || normalizedName === \"month\".concat(i)) {\n          mFields[i - 1] = col.name;\n        } else if (normalizedName === \"m\".concat(i, \"paid\") || normalizedName === \"m\".concat(i, \"ispaid\") || normalizedName === \"month\".concat(i, \"paid\") || normalizedName === \"month\".concat(i, \"ispaid\") || normalizedName === \"ispaidm\".concat(i) || normalizedName === \"paidm\".concat(i) || normalizedName === \"costpaidm\".concat(i) || normalizedName === \"m\".concat(i, \"costpaid\")) {\n          monthPaidFields[i - 1] = col.name;\n        } else if (normalizedName === \"m\".concat(i, \"hascomments\") || normalizedName === \"m\".concat(i, \"hascomment\") || normalizedName === \"month\".concat(i, \"hascomments\") || normalizedName === \"month\".concat(i, \"hascomment\") || normalizedName === \"hascommentsm\".concat(i) || normalizedName === \"hascommentm\".concat(i)) {\n          monthHasCommentFields[i - 1] = col.name;\n        } else if (normalizedName === \"m\".concat(i, \"commenttooltip\") || normalizedName === \"m\".concat(i, \"comments\") || normalizedName === \"m\".concat(i, \"comment\") || normalizedName === \"month\".concat(i, \"commenttooltip\") || normalizedName === \"month\".concat(i, \"comments\") || normalizedName === \"month\".concat(i, \"comment\") || normalizedName === \"commentm\".concat(i) || normalizedName === \"commentsm\".concat(i)) {\n          monthCommentTooltipFields[i - 1] = col.name;\n        }\n      }\n    });\n    // Pass-1 buckets, keyed by normalized RowId so pass 2 can resolve ParentId\n    // references in O(1). Contracts go to a plain list — nothing references a\n    // contract by id, so no map is needed for them.\n    var accountsMap = new Map();\n    var subaccountsMap = new Map();\n    var contractsList = [];\n    // Parse records\n    dataset.sortedRecordIds.forEach(id => {\n      var record = dataset.records[id];\n      // ---- Core identity fields ----\n      // rowId falls back to the PCF record id (`|| id`) so a row missing RowId\n      // still gets a stable map key. normalizeId trims/lowercases GUIDs so the\n      // ParentId → RowId matching in pass 2 survives the mixed casing that\n      // Dataverse and Canvas produce; BOTH sides must use the same normalization.\n      var type = this.parseText(this.firstAvailableValue(record, [typeField, \"Type\", \"type\"]));\n      var rowId = this.normalizeId(this.firstAvailableValue(record, [rowIdField, \"RowId\", \"rowId\", \"ID\", \"id\"]) || id);\n      var parentId = this.normalizeId(this.firstAvailableValue(record, [parentIdField, \"ParentId\", \"parentId\"]));\n      var num = this.parseText(this.firstAvailableValue(record, [numberField, \"Number\", \"number\"]));\n      // SubLabel can carry ||KEY=value|| metadata tokens (||STD=..||, ||PAID=..||).\n      // Keep rawSubLabel for token extraction below; strip the tokens from the\n      // user-visible subLabel so the metadata never renders in the grid.\n      var rawSubLabel = this.parseText(this.firstAvailableValue(record, [subLabelField, \"SubLabel\", \"subLabel\"]));\n      var subLabel = this.removeMetaTokens(rawSubLabel);\n      // CostPaidBy is DevCo or SPV. If the dedicated column is missing/empty it\n      // is recoverable from a \"[DevCo]\"/\"[SPV]\" tag inside SubLabel — hence the\n      // cleaned subLabel is passed in as fallback search text.\n      var costPaidBy = this.parseCostPaidBy(this.firstAvailableValue(record, [costPaidByField, \"CostPaidBy\", \"costPaidBy\", \"CostPaidByText\", \"CostType\", \"costType\"]), subLabel);\n      var name = this.parseText(this.firstAvailableValue(record, [nameField, \"Name\", \"name\"]));\n      // Fallbacks for name in case field mappings are different per type\n      if (!name) {\n        name = this.parseText(this.firstAvailableValue(record, [\"vsb_costdescription\", \"vsb_name\", \"Name\", \"name\"]));\n      }\n      // ---- Standard-contract detection: three independent paths, OR-ed ----\n      // 1) any boolean-ish value from the alias columns below (choice columns\n      //    arrive as bool/0-1/label/object depending on the binding),\n      // 2) a ||STD=1|| (or ||STD=true||) metadata token inside SubLabel,\n      // 3) a name starting with \"Standard \" as a last-resort fallback.\n      // All three exist because no single source is reliable across Canvas and\n      // Dataverse shapes — do not remove a path without testing every source.\n      var rawIsStandardContract = this.firstAvailableValue(record, [isStandardContractField, \"IsStandardContract\", \"isStandardContract\", \"IsStandard\", \"isStandard\", \"StandardCost\", \"standardCost\", \"StandardContract\", \"standardContract\"]);\n      var standardMeta = this.extractMetaValue(rawSubLabel, \"STD\");\n      var isStandardContract = this.parseBoolean(rawIsStandardContract) || standardMeta === \"1\" || standardMeta.toLowerCase() === \"true\" || name.toLowerCase().trim().startsWith(\"standard \");\n      // DistributionType (Individual / Equal / Cluster-distributed) decides\n      // whether the month paid/unpaid action is allowed downstream. When no\n      // dedicated column exists, recover it by scanning the SubLabel text.\n      var distributionType = this.parseText(this.firstAvailableValue(record, [distributionTypeField, \"DistributionType\", \"distributionType\", \"Distribution\", \"distribution\"])) || this.parseDistributionTypeFromText(subLabel);\n      // M1..M12 = monthly costs for the SELECTED YEAR ONLY. These feed the month\n      // cells, never the all-years totals (see the TotalCost handling below).\n      var months = mFields.map((fieldName, index) => this.parseNumber(this.firstAvailableValue(record, [fieldName, \"M\".concat(index + 1), \"m\".concat(index + 1), \"Month\".concat(index + 1), \"month\".concat(index + 1)])));\n      // Paid-month flags: try the detected column first, then every known alias\n      // spelling (M{n}Paid, Month{n}Paid, M{n}IsPaid, PaidM{n}, CostPaidM{n},\n      // M{n}CostPaid) because different Canvas iterations used different names.\n      var monthPaidFromFields = monthPaidFields.map((fieldName, index) => {\n        var monthNo = index + 1;\n        return this.parseBoolean(this.firstAvailableValue(record, [fieldName, \"M\".concat(monthNo, \"Paid\"), \"m\".concat(monthNo, \"Paid\"), \"Month\".concat(monthNo, \"Paid\"), \"month\".concat(monthNo, \"Paid\"), \"M\".concat(monthNo, \"IsPaid\"), \"PaidM\".concat(monthNo), \"CostPaidM\".concat(monthNo), \"M\".concat(monthNo, \"CostPaid\")]));\n      });\n      // A ||PAID=1,2,3|| SubLabel token is an alternative paid-month source; a\n      // month counts as paid if EITHER the column OR the token says so.\n      var monthPaidFromSubLabel = this.parsePaidMonthsFromSubLabel(rawSubLabel);\n      var monthPaid = monthPaidFromFields.map((fieldValue, index) => fieldValue || monthPaidFromSubLabel[index]);\n      // Contract-level tooltip text. parseCommentTooltip rejects boolean-looking\n      // values so a HasComments flag accidentally bound to one of these aliases\n      // cannot render \"true\" as the tooltip.\n      var commentTooltip = this.parseCommentTooltip(this.firstAvailableValue(record, [commentTooltipField, \"CommentTooltip\", \"commentTooltip\", \"GeneralCommentTooltip\", \"generalCommentTooltip\", \"ContractCommentTooltip\", \"contractCommentTooltip\", \"CommentText\", \"commentText\", \"CommentDescription\", \"commentDescription\", \"GeneralComment\", \"generalComment\", \"ContractComment\", \"contractComment\", \"LatestComment\", \"latestComment\", \"Comment\", \"comment\", \"Comments\", \"comments\"]));\n      // Month-level tooltips (M{n}CommentTooltip and friends). Computed BEFORE\n      // monthHasComments because a non-empty tooltip implies the month has\n      // comments even when the explicit flag column is absent.\n      var monthCommentTooltips = monthCommentTooltipFields.map((fieldName, index) => {\n        var monthNo = index + 1;\n        return this.parseCommentTooltip(this.firstAvailableValue(record, [fieldName, \"M\".concat(monthNo, \"CommentTooltip\"), \"m\".concat(monthNo, \"CommentTooltip\"), \"M\".concat(monthNo, \"Comment\"), \"m\".concat(monthNo, \"Comment\"), \"M\".concat(monthNo, \"Comments\"), \"m\".concat(monthNo, \"Comments\"), \"Month\".concat(monthNo, \"CommentTooltip\"), \"month\".concat(monthNo, \"CommentTooltip\"), \"Month\".concat(monthNo, \"Comment\"), \"month\".concat(monthNo, \"Comment\"), \"Month\".concat(monthNo, \"Comments\"), \"month\".concat(monthNo, \"Comments\"), \"CommentM\".concat(monthNo), \"commentM\".concat(monthNo), \"CommentsM\".concat(monthNo), \"commentsM\".concat(monthNo)]));\n      });\n      // Month \"has comments\" = explicit flag (any alias) OR a non-empty tooltip\n      // for that month — flag columns are frequently missing from the collection.\n      var monthHasComments = monthHasCommentFields.map((fieldName, index) => {\n        var monthNo = index + 1;\n        return this.parseBoolean(this.firstAvailableValue(record, [fieldName, \"M\".concat(monthNo, \"HasComments\"), \"m\".concat(monthNo, \"HasComments\"), \"M\".concat(monthNo, \"HasComment\"), \"m\".concat(monthNo, \"HasComment\"), \"Month\".concat(monthNo, \"HasComments\"), \"month\".concat(monthNo, \"HasComments\"), \"Month\".concat(monthNo, \"HasComment\"), \"month\".concat(monthNo, \"HasComment\"), \"HasCommentsM\".concat(monthNo), \"hasCommentsM\".concat(monthNo), \"HasCommentM\".concat(monthNo), \"hasCommentM\".concat(monthNo)])) || monthCommentTooltips[index].length > 0;\n      });\n      // ---- ALL-YEARS totals (critical semantics) ----\n      // TotalCost / PlannedCost / ActualCost are computed in Canvas across ALL\n      // years. Summing M1..M12 here would only cover the selected year — that\n      // exact mistake was a past bug — so visibleYearTotal is used ONLY when the\n      // TotalCost column is genuinely absent/empty for the row.\n      var visibleYearTotal = months.reduce((sum, monthCost) => sum + monthCost, 0);\n      var rawTotal = this.firstAvailableValue(record, [totalCostField, \"TotalCost\", \"totalCost\", \"TotalCosts\", \"totalCosts\"]);\n      var total = rawTotal === null || rawTotal === undefined || rawTotal === \"\" ? visibleYearTotal : this.parseNumber(rawTotal);\n      var planned = this.parseNumber(this.firstAvailableValue(record, [plannedCostField, \"PlannedCost\", \"plannedCost\", \"TotalPlannedCost\", \"totalPlannedCost\"]));\n      var actual = this.parseNumber(this.firstAvailableValue(record, [actualCostField, \"ActualCost\", \"actualCost\", \"TotalActualCost\", \"totalActualCost\"]));\n      // Dynamically search for comment markers\n      // Contract-level \"has comments\": first via alias list, then by re-scanning\n      // the actual columns case-insensitively (covers a Comments column the alias\n      // probe could miss). NOTE: the column re-scan OVERWRITES the alias result.\n      // Finally, a non-empty tooltip always implies comments exist.\n      var hasComments = this.parseBoolean(this.firstAvailableValue(record, [\"HasComments\", \"hasComments\", \"Comments\", \"comments\", \"Comment\", \"comment\"]));\n      columns.forEach(col => {\n        var lowerCol = col.name.toLowerCase();\n        if (lowerCol === \"hascomments\" || lowerCol === \"comments\" || lowerCol === \"comment\") {\n          var rawVal = this.safeGetValue(record, col.name);\n          hasComments = rawVal === true || rawVal === \"true\" || rawVal === 1 || Number(rawVal) > 0;\n        }\n      });\n      hasComments = hasComments || commentTooltip.length > 0;\n      // Determine item type\n      // The Type column (\"account\"/\"subaccount\"/\"contract\") normally decides the\n      // bucket. Legacy datasets without Type fall back to IsAccount/IsContract\n      // flags, defaulting to \"subaccount\" when neither flag is set — changing\n      // this default reclassifies untyped rows and breaks the hierarchy.\n      var finalType = type.toLowerCase();\n      if (!finalType) {\n        var isAccountVal = isAccountField ? this.safeGetValue(record, isAccountField) : null;\n        var isContractVal = isContractField ? this.safeGetValue(record, isContractField) : null;\n        var isAccount = isAccountVal === true || isAccountVal === \"true\" || isAccountVal === 1;\n        var isContract = isContractVal === true || isContractVal === \"true\" || isContractVal === 1;\n        if (isAccount) {\n          finalType = \"account\";\n        } else if (isContract) {\n          finalType = \"contract\";\n        } else {\n          finalType = \"subaccount\";\n        }\n      }\n      if (finalType === \"account\") {\n        accountsMap.set(rowId, {\n          id: rowId,\n          number: num,\n          name: name,\n          totalCost: total,\n          plannedCost: planned,\n          actualCost: actual,\n          months: months,\n          subaccounts: []\n        });\n      } else if (finalType === \"subaccount\") {\n        subaccountsMap.set(rowId, {\n          id: rowId,\n          parentId: parentId,\n          number: num,\n          name: name,\n          totalCost: total,\n          plannedCost: planned,\n          actualCost: actual,\n          months: months,\n          // Options embedded inline on the subaccount row (JSON or\n          // \"id|name;id2|name2\" text); the separate StandardContractOptions\n          // dataset (parseStandardContractOptionsDataset) can supply them too.\n          standardContractOptions: this.parseStandardContractOptions(this.firstAvailableValue(record, [standardContractOptionsField, \"StandardContractOptions\", \"standardContractOptions\", \"StandardContracts\", \"standardContracts\", \"AvailableStandardContracts\", \"availableStandardContracts\"])),\n          contracts: []\n        });\n      } else if (finalType === \"contract\") {\n        contractsList.push({\n          id: rowId,\n          parentId: parentId,\n          number: num,\n          name: name,\n          subLabel: subLabel,\n          costPaidBy: costPaidBy,\n          totalCost: total,\n          plannedCost: planned,\n          actualCost: actual,\n          hasComments: hasComments,\n          commentTooltip: commentTooltip,\n          isStandardContract: isStandardContract,\n          distributionType: distributionType,\n          months: months,\n          monthPaid: monthPaid,\n          monthHasComments: monthHasComments,\n          monthCommentTooltips: monthCommentTooltips\n        });\n      }\n    });\n    // ---- Pass 2: stitch children onto parents via normalized ParentId ----\n    // A contract/subaccount whose ParentId matches no known RowId is silently\n    // discarded (no orphan bucket, no warning). If rows vanish from the grid,\n    // first check ParentId values/casing in the Canvas collection.\n    // Nest relations (Contracts -> Subaccounts)\n    contractsList.forEach(c => {\n      var sub = subaccountsMap.get(c.parentId);\n      if (sub) sub.contracts.push(c);\n    });\n    // Nest relations (Subaccounts -> Accounts)\n    subaccountsMap.forEach(sub => {\n      var acc = accountsMap.get(sub.parentId);\n      if (acc) acc.subaccounts.push(sub);\n    });\n    // Map preserves insertion order, so accounts render in dataset (Canvas) order.\n    return Array.from(accountsMap.values());\n  }\n  /**\n   * Parses the secondary `StandardContractOptions` dataset (Canvas collection\n   * `colStandardContractOptionsForPCF`) into a lookup keyed by SUBACCOUNT RowId.\n   *\n   * Each row represents one standard contract offered for one subaccount:\n   *   - StandardContractId — aliases include the Dataverse primary key\n   *     vsb_devexcapexstandardassumptionsid / devexcapexstandardassumptions / id.\n   *   - SubaccountId — aliases vsb_subaccountid / capexaccountlist / subaccount.\n   *     This value MUST equal the subaccount's RowId in the main dataset (after\n   *     normalizeId lowercasing) or the option will never appear in the grid.\n   *   - Name — aliases description / vsb_description / itemdisplayname; when the\n   *     name is blank, a vsb_autoid column synthesises \"Standard Contract {n}\".\n   *\n   * Rows missing id, subaccountId or name are skipped entirely. Returns an empty\n   * map while the dataset is still loading so callers render without options\n   * instead of crashing mid-refresh.\n   */\n  static parseStandardContractOptionsDataset(dataset) {\n    var optionsBySubaccount = new Map();\n    if (!dataset || dataset.loading) {\n      return optionsBySubaccount;\n    }\n    var standardContractIdField = \"StandardContractId\";\n    var subaccountIdField = \"SubaccountId\";\n    var nameField = \"Name\";\n    var autoIdField = \"\";\n    dataset.columns.forEach(col => {\n      var lowerName = col.name.toLowerCase();\n      var normalizedName = this.normalizeFieldName(col.name);\n      if (normalizedName === \"standardcontractid\" || normalizedName === \"vsbdevexcapexstandardassumptionsid\" || normalizedName === \"devexcapexstandardassumptions\" || normalizedName === \"devexcapexstandardassumptionsid\" || normalizedName === \"id\") {\n        standardContractIdField = col.name;\n      } else if (normalizedName === \"subaccountid\" || normalizedName === \"vsbsubaccountid\" || normalizedName === \"capexaccountlist\" || normalizedName === \"subaccount\") {\n        subaccountIdField = col.name;\n      } else if (normalizedName === \"name\" || normalizedName === \"description\" || normalizedName === \"vsbdescription\" || normalizedName === \"descriptioninput\" || normalizedName === \"itemdisplayname\") {\n        nameField = col.name;\n      } else if (normalizedName === \"autoid\" || lowerName === \"vsb_autoid\") {\n        autoIdField = col.name;\n      }\n    });\n    dataset.sortedRecordIds.forEach(recordId => {\n      var record = dataset.records[recordId];\n      // Ids are normalized to lowercase so they match the subaccount RowIds\n      // produced by parseDatasetToTree; the PCF record id is the id fallback.\n      // (Raw getValue is used here, unlike the safeGetValue probing elsewhere,\n      // because the resolved/default column names are expected to exist.)\n      var id = this.normalizeId(record.getValue(standardContractIdField) || recordId);\n      var subaccountId = this.normalizeId(record.getValue(subaccountIdField));\n      var autoId = autoIdField ? (record.getValue(autoIdField) || \"\").toString().trim() : \"\";\n      var name = (record.getValue(nameField) || (autoId ? \"Standard Contract \".concat(autoId) : \"\")).toString().trim();\n      // Incomplete rows are dropped: an option without a subaccount link or a\n      // display name could never be selected/rendered meaningfully anyway.\n      if (!id || !subaccountId || !name) {\n        return;\n      }\n      var existingOptions = optionsBySubaccount.get(subaccountId) || [];\n      existingOptions.push({\n        id,\n        name\n      });\n      optionsBySubaccount.set(subaccountId, existingOptions);\n    });\n    return optionsBySubaccount;\n  }\n  /**\n   * Parses standard-contract options embedded in a SINGLE dataset cell on a\n   * subaccount row. Two encodings are accepted because Canvas changed the\n   * format over time:\n   *   1) a JSON array of objects — ids under id/Id/guid/\n   *      ItemGUIDForLoadingStandardContract, names under name/Name/description/\n   *      ItemDisplayName;\n   *   2) legacy delimited text \"id|name;id2|name2\" (the name part may itself\n   *      contain \"|\", hence the nameParts.join(\"|\") below).\n   * Ids are lowercased to stay comparable with normalizeId() output elsewhere.\n   * Options missing either id or name are filtered out.\n   */\n  static parseStandardContractOptions(value) {\n    if (value === null || value === undefined) {\n      return [];\n    }\n    var rawValue = value.toString().trim();\n    if (!rawValue) {\n      return [];\n    }\n    try {\n      var parsed = JSON.parse(rawValue);\n      if (Array.isArray(parsed)) {\n        return parsed.map(item => ({\n          id: (item.id || item.Id || item.guid || item.ItemGUIDForLoadingStandardContract || \"\").toString().trim().toLowerCase(),\n          name: (item.name || item.Name || item.description || item.ItemDisplayName || \"\").toString().trim()\n        })).filter(option => option.id && option.name);\n      }\n    } catch (_a) {\n      // Non-JSON values are parsed below as id|name;id2|name2.\n    }\n    return rawValue.split(\";\").map(part => {\n      var _part$split = part.split(\"|\"),\n        _part$split2 = _toArray(_part$split),\n        id = _part$split2[0],\n        nameParts = _arrayLikeToArray(_part$split2).slice(1);\n      return {\n        id: (id || \"\").trim().toLowerCase(),\n        name: nameParts.join(\"|\").trim()\n      };\n    }).filter(option => option.id && option.name);\n  }\n  /**\n   * getValue() that never throws. The PCF dataset API may THROW or return\n   * undefined for columns that are not part of the dataset (behaviour varies by\n   * host/runtime version), and this parser deliberately probes many speculative\n   * alias names per record — so every probe must be exception-safe. An empty\n   * fieldName (an undetected optional column) short-circuits to null.\n   */\n  static safeGetValue(record, fieldName) {\n    if (!fieldName) {\n      return null;\n    }\n    try {\n      return record.getValue(fieldName);\n    } catch (_a) {\n      return null;\n    }\n  }\n  /**\n   * getFormattedValue() that never throws, for the same reason as safeGetValue:\n   * probing alias columns that may not exist must not crash the whole parse.\n   * Normalizes null/undefined to \"\" so callers can treat \"\" as \"no value\".\n   */\n  static safeGetFormattedValue(record, fieldName) {\n    if (!fieldName) {\n      return \"\";\n    }\n    try {\n      return record.getFormattedValue(fieldName) || \"\";\n    } catch (_a) {\n      return \"\";\n    }\n  }\n  /**\n   * Returns the first non-empty value among candidate column names — callers\n   * pass the column resolved from the dataset first, then hard-coded aliases.\n   * Per candidate, the RAW value is preferred (preserves numbers/booleans/choice\n   * objects) and the FORMATTED value is only a fallback, because some hosts only\n   * populate formatted text (e.g. choice labels). null/undefined/\"\" count as\n   * \"missing\" so later aliases still get a chance; empty names are skipped\n   * (that is what makes the \"\"-default field variables safe to pass in).\n   */\n  static firstAvailableValue(record, fieldNames) {\n    for (var fieldName of fieldNames) {\n      if (!fieldName) {\n        continue;\n      }\n      var value = this.safeGetValue(record, fieldName);\n      if (value !== null && value !== undefined && value !== \"\") {\n        return value;\n      }\n      var formattedValue = this.safeGetFormattedValue(record, fieldName);\n      if (formattedValue) {\n        return formattedValue;\n      }\n    }\n    return null;\n  }\n  /**\n   * Reads one `||KEY=value||` metadata token out of a SubLabel string. These\n   * tokens let Canvas piggyback extra flags (STD, PAID, ...) on an existing text\n   * column when adding a real column to the collection is impractical. The key\n   * is regex-escaped, matching is case-insensitive, and the value cannot\n   * contain \"|\" (it would terminate the token). Returns \"\" when absent.\n   */\n  static extractMetaValue(source, key) {\n    if (!source || !key) {\n      return \"\";\n    }\n    var escapedKey = key.replace(/[.*+?^${}()|[\\]\\\\]/g, \"\\\\$&\");\n    var match = source.match(new RegExp(\"\\\\|\\\\|\\\\s*\".concat(escapedKey, \"\\\\s*=\\\\s*([^|]+?)\\\\s*\\\\|\\\\|\"), \"i\"));\n    return match ? match[1].trim() : \"\";\n  }\n  /**\n   * Strips ALL ||KEY=value|| tokens from SubLabel so end users never see the\n   * smuggled metadata, then collapses leftover whitespace. Must stay in sync\n   * with the token format consumed by extractMetaValue.\n   */\n  static removeMetaTokens(source) {\n    return (source || \"\").replace(/\\|\\|\\s*[^|=]+?\\s*=\\s*[^|]*?\\s*\\|\\|/g, \"\").replace(/\\s+/g, \" \").trim();\n  }\n  /**\n   * Decodes a ||PAID=1,2,3|| SubLabel token into a 12-slot boolean array\n   * (index 0 = January). Out-of-range or non-integer entries are ignored rather\n   * than failing the row. Result is OR-ed with the M{n}Paid columns by the\n   * caller, so this is purely an additional \"paid\" source, never a veto.\n   */\n  static parsePaidMonthsFromSubLabel(subLabel) {\n    var paidMonths = Array(12).fill(false);\n    var paidMeta = this.extractMetaValue(subLabel, \"PAID\");\n    if (!paidMeta) {\n      return paidMonths;\n    }\n    paidMeta.split(\",\").map(part => Number(part.trim())).filter(monthNo => Number.isInteger(monthNo) && monthNo >= 1 && monthNo <= 12).forEach(monthNo => {\n      paidMonths[monthNo - 1] = true;\n    });\n    return paidMonths;\n  }\n  /**\n   * Last-resort DistributionType recovery from free text (the cleaned SubLabel)\n   * when no dedicated column exists. Only the canonical strings \"Equal\n   * Distribution\" / \"Individual Distribution\" are returned because downstream\n   * paid/unpaid gating compares against those exact values. NOTE: the cluster\n   * wordings (\"cluster distribution\"/\"cluster-distributed\") currently map to\n   * \"Equal Distribution\" — they are treated identically by the gating logic;\n   * change only together with that logic in the grid.\n   */\n  static parseDistributionTypeFromText(value) {\n    var normalized = (value || \"\").toLowerCase();\n    if (normalized.includes(\"equal distribution\") || normalized.includes(\"cluster distribution\") || normalized.includes(\"cluster-distributed\")) {\n      return \"Equal Distribution\";\n    }\n    if (normalized.includes(\"individual distribution\")) {\n      return \"Individual Distribution\";\n    }\n    return \"\";\n  }\n  /**\n   * Tolerant boolean for Canvas/Dataverse choice and yes/no columns, which can\n   * arrive as real booleans, 0/1 numbers, \"true\"/\"yes\"/\"ja\" strings (German UI\n   * in production!), \"paid\"/\"unpaid\" labels, choice OBJECTS (unwrapped via\n   * Value/Name/Label/DisplayName), or labels merely containing \"standard\".\n   * The negative variants (\"not standard\"/\"non standard\"/\"non-standard\") are\n   * checked BEFORE the positive includes(\"standard\") so \"Non-Standard\" cannot\n   * be misread as true. Empty/unrecognized text returns false — the safe\n   * default is \"not paid / not standard\".\n   */\n  static parseBoolean(value) {\n    var _a, _b, _c, _d, _e, _f, _g;\n    if (value === true || value === 1) {\n      return true;\n    }\n    if (value === false || value === 0) {\n      return false;\n    }\n    if (value && typeof value === \"object\") {\n      var record = value;\n      return this.parseBoolean((_g = (_f = (_e = (_d = (_c = (_b = (_a = record.Value) !== null && _a !== void 0 ? _a : record.value) !== null && _b !== void 0 ? _b : record.Name) !== null && _c !== void 0 ? _c : record.name) !== null && _d !== void 0 ? _d : record.Label) !== null && _e !== void 0 ? _e : record.label) !== null && _f !== void 0 ? _f : record.DisplayName) !== null && _g !== void 0 ? _g : record.displayName);\n    }\n    var normalized = (value || \"\").toString().trim().toLowerCase();\n    if (!normalized || normalized === \"false\" || normalized === \"no\" || normalized === \"nein\" || normalized === \"unpaid\" || normalized === \"0\" || normalized.includes(\"not standard\") || normalized.includes(\"non standard\") || normalized.includes(\"non-standard\")) {\n      return false;\n    }\n    return normalized === \"true\" || normalized === \"yes\" || normalized === \"ja\" || normalized === \"paid\" || normalized === \"1\" || normalized.includes(\"standard\");\n  }\n  /**\n   * Normalizes any cell to trimmed text, unwrapping choice/lookup objects via\n   * their common label properties first so \"[object Object]\" never leaks into\n   * the UI when Canvas hands over a choice column instead of plain text.\n   */\n  static parseText(value) {\n    var _a, _b, _c, _d, _e, _f, _g;\n    if (value === null || value === undefined) {\n      return \"\";\n    }\n    if (typeof value === \"object\") {\n      var record = value;\n      return this.parseText((_g = (_f = (_e = (_d = (_c = (_b = (_a = record.Value) !== null && _a !== void 0 ? _a : record.value) !== null && _b !== void 0 ? _b : record.Name) !== null && _c !== void 0 ? _c : record.name) !== null && _d !== void 0 ? _d : record.Label) !== null && _e !== void 0 ? _e : record.label) !== null && _f !== void 0 ? _f : record.DisplayName) !== null && _g !== void 0 ? _g : record.displayName);\n    }\n    return value.toString().trim();\n  }\n  /**\n   * Extracts tooltip TEXT only. Booleans, numbers and boolean-looking strings\n   * (\"true\"/\"yes\"/\"1\"/...) are rejected as \"\" because the many tooltip aliases\n   * overlap with HasComments-style flag columns — without this filter a flag\n   * misbound to a tooltip alias would literally render \"true\" as the tooltip.\n   */\n  static parseCommentTooltip(value) {\n    if (value === null || value === undefined || typeof value === \"boolean\" || typeof value === \"number\") {\n      return \"\";\n    }\n    var text = this.parseText(value);\n    var normalized = text.toLowerCase();\n    if (!text || normalized === \"true\" || normalized === \"false\" || normalized === \"yes\" || normalized === \"no\" || normalized === \"1\" || normalized === \"0\") {\n      return \"\";\n    }\n    return text;\n  }\n  /**\n   * Tolerant numeric parse for cost cells. Strips whitespace and comma\n   * thousands-separators because firstAvailableValue may fall back to\n   * getFormattedValue, which returns locale-formatted text like \"1,234.50\".\n   * Anything unparseable becomes 0 so cost aggregation never produces NaN.\n   */\n  static parseNumber(value) {\n    if (typeof value === \"number\") {\n      return Number.isFinite(value) ? value : 0;\n    }\n    var normalized = (value || \"\").toString().trim().replace(/\\s/g, \"\");\n    if (!normalized) {\n      return 0;\n    }\n    var parsed = Number(normalized.replace(/,/g, \"\"));\n    return Number.isFinite(parsed) ? parsed : 0;\n  }\n  /**\n   * Collapses a column name to lowercase alphanumerics, e.g.\n   * \"vsb_Is-Standard Contract\" → \"vsbisstandardcontract\". This single rule is\n   * what lets Canvas display names, Dataverse logical names and ad-hoc\n   * collection column names all hit the same alias tables — every alias list\n   * in this file is written in this normalized form, so changing the regex\n   * invalidates ALL of them at once.\n   */\n  static normalizeFieldName(value) {\n    return value.toLowerCase().replace(/[^a-z0-9]/g, \"\");\n  }\n  /**\n   * Canonicalizes RowId/ParentId/GUID values to trimmed LOWERCASE strings,\n   * unwrapping lookup objects (id/Id/ID/guid/Guid/GUID) first. The hierarchy\n   * stitching and the StandardContractOptions → subaccount matching compare\n   * these strings directly, so BOTH sides of every link must pass through this\n   * method — Dataverse returns GUIDs with different casing depending on API.\n   */\n  static normalizeId(value) {\n    if (value === null || value === undefined) {\n      return \"\";\n    }\n    if (typeof value === \"object\") {\n      var record = value;\n      var possibleId = record.id || record.Id || record.ID || record.guid || record.Guid || record.GUID;\n      if (possibleId !== null && possibleId !== undefined) {\n        return possibleId.toString().trim().toLowerCase();\n      }\n    }\n    return value.toString().trim().toLowerCase();\n  }\n  /**\n   * Maps the cost bearer to the canonical \"DevCo\"/\"SPV\" spellings the grid\n   * filters/renders on. When the dedicated column is empty the cleaned\n   * SubLabel is scanned instead, which is how a \"[DevCo]\"/\"[SPV]\" tag embedded\n   * in SubLabel is recovered. Unrecognized non-empty values are returned\n   * verbatim so a new category surfaces in the UI instead of disappearing.\n   */\n  static parseCostPaidBy(value, subLabel) {\n    var rawValue = (value !== null && value !== void 0 ? value : \"\").toString().trim();\n    var fallbackValue = subLabel || \"\";\n    var normalized = (rawValue || fallbackValue).toLowerCase();\n    if (normalized.includes(\"devco\") || normalized.includes(\"dev co\")) {\n      return \"DevCo\";\n    }\n    if (normalized.includes(\"spv\")) {\n      return \"SPV\";\n    }\n    return rawValue;\n  }\n  /**\n   * Parses a cluster start date and truncates it to LOCAL midnight so cluster\n   * boundary math is date-only — a time-of-day or timezone offset coming from\n   * the Dataverse/PCF date property must not shift a cluster into the\n   * neighbouring month. Invalid/empty input returns null, which callers treat\n   * as \"cluster not configured\".\n   */\n  static normalizeDate(value) {\n    if (!value) {\n      return null;\n    }\n    var parsed = value instanceof Date ? value : new Date(value);\n    if (Number.isNaN(parsed.getTime())) {\n      return null;\n    }\n    return new Date(parsed.getFullYear(), parsed.getMonth(), parsed.getDate());\n  }\n  /**\n   * Builds month-by-month cluster assignment and grouped spans for header rendering.\n   *\n   * Inputs are the StartYear and Cluster1..6Date PCF properties (set by Canvas).\n   * Each configured cluster is mapped to the month (0..11) of the SELECTED year\n   * where it becomes active:\n   *   - starts before StartYear  → month 0 (already active in January)\n   *   - starts during StartYear  → its actual calendar month\n   *   - starts after StartYear   → sentinel 12, filtered out (never visible)\n   * A cluster then stays active until the next cluster's start month. With no\n   * visible clusters the whole year becomes a single clusterIndex-0 span, which\n   * the header renders as \"no cluster\".\n   */\n  static calculateClusterTimeline(context) {\n    var _a, _b, _c, _d, _e, _f, _g;\n    var startYear = ((_a = context.parameters.StartYear) === null || _a === void 0 ? void 0 : _a.raw) || 2026;\n    var clusterStarts = [(_b = context.parameters.Cluster1Date) === null || _b === void 0 ? void 0 : _b.raw, (_c = context.parameters.Cluster2Date) === null || _c === void 0 ? void 0 : _c.raw, (_d = context.parameters.Cluster3Date) === null || _d === void 0 ? void 0 : _d.raw, (_e = context.parameters.Cluster4Date) === null || _e === void 0 ? void 0 : _e.raw, (_f = context.parameters.Cluster5Date) === null || _f === void 0 ? void 0 : _f.raw, (_g = context.parameters.Cluster6Date) === null || _g === void 0 ? void 0 : _g.raw].map((raw, idx) => {\n      var normalized = this.normalizeDate(raw);\n      if (!normalized) {\n        return null;\n      }\n      var year = normalized.getFullYear();\n      // 12 is the \"starts after the selected year\" sentinel; such clusters\n      // are filtered out below so they cannot affect this year's header.\n      var startMonth = 12;\n      if (year < startYear) {\n        startMonth = 0;\n      } else if (year === startYear) {\n        startMonth = normalized.getMonth();\n      }\n      return {\n        clusterIndex: idx + 1,\n        startMonth\n      };\n    }).filter(entry => entry !== null && entry.startMonth < 12)\n    // Earliest start first; ties broken by cluster number so the header is\n    // stable when two clusters share a start month.\n    .sort((a, b) => a.startMonth - b.startMonth || a.clusterIndex - b.clusterIndex);\n    if (clusterStarts.length === 0) {\n      var emptyClusters = Array(12).fill(0);\n      return {\n        monthClusters: emptyClusters,\n        spans: [{\n          clusterIndex: 0,\n          length: 12\n        }]\n      };\n    }\n    // Months BEFORE the first visible cluster's start are pre-filled with that\n    // first cluster's index — the header has no \"no cluster yet\" state mid-year,\n    // so the earliest cluster claims the leading months by design.\n    var monthClusters = Array(12).fill(clusterStarts[0].clusterIndex);\n    var activeCluster = clusterStarts[0].clusterIndex;\n    var nextStartIdx = 1;\n    // Sweep Jan→Dec: the `while` (not `if`) lets several clusters start in the\n    // same month — the last one in sorted order wins for that month onward.\n    for (var month = 0; month < 12; month++) {\n      while (nextStartIdx < clusterStarts.length && month >= clusterStarts[nextStartIdx].startMonth) {\n        activeCluster = clusterStarts[nextStartIdx].clusterIndex;\n        nextStartIdx++;\n      }\n      monthClusters[month] = activeCluster;\n    }\n    // Run-length encode the clusters to build spanned cells\n    var spans = [];\n    var currentCluster = monthClusters[0];\n    var currentLength = 1;\n    for (var i = 1; i < 12; i++) {\n      if (monthClusters[i] === currentCluster) {\n        currentLength++;\n      } else {\n        spans.push({\n          clusterIndex: currentCluster,\n          length: currentLength\n        });\n        currentCluster = monthClusters[i];\n        currentLength = 1;\n      }\n    }\n    spans.push({\n      clusterIndex: currentCluster,\n      length: currentLength\n    });\n    return {\n      monthClusters,\n      spans\n    };\n  }\n  /**\n   * Determines grouped cluster spans for header rendering.\n   *\n   * Thin convenience wrapper kept for callers that only need the header spans\n   * and not the per-month cluster map of calculateClusterTimeline.\n   */\n  static calculateClusterSpans(context) {\n    return this.calculateClusterTimeline(context).spans;\n  }\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./TableConnectedToDataversePCF/services/DatasetParser.ts?\n}");

/***/ },

/***/ "./TableConnectedToDataversePCF/ui/ActionDropdown.ts"
/*!***********************************************************!*\
  !*** ./TableConnectedToDataversePCF/ui/ActionDropdown.ts ***!
  \***********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ActionDropdown: () => (/* binding */ ActionDropdown)\n/* harmony export */ });\n/**\n * =============================================================================\n * ActionDropdown.ts — three-dot (kebab) context menu for contract rows\n * =============================================================================\n * Presents Edit / Comments / Delete for the contract row whose \"...\" button\n * was clicked. It performs NO business logic itself: each item simply invokes\n * the callback supplied by index.ts (triggerAction), which raises the matching\n * output trigger so the Canvas App's OnChange performs the real Dataverse\n * operation — keeping this class purely visual.\n *\n * DESIGN DECISIONS:\n *  - ONE shared dropdown instance for the whole grid, repositioned next to\n *    whichever row invoked it. With potentially hundreds of contract rows,\n *    one menu per row would multiply DOM nodes and listeners for an element\n *    that can only ever be open once; _currentRecordId tracks \"whose menu is\n *    this right now\".\n *  - The element is appended to document.body, NOT the PCF container, so it\n *    escapes the grid's scroll container (overflow clipping) and any\n *    stacking-context/z-index traps — see note in createDropdownMenu().\n *  - Because it lives on document.body, destroy() MUST remove it manually;\n *    the framework only disposes the control's own container.\n * =============================================================================\n */\n/**\n * Handles the rendering and events for the context action dropdown menu.\n */\nclass ActionDropdown {\n  /**\n   * @param onActionTriggered Callback into the root control; keeping the\n   * action wiring injected (instead of importing index.ts) avoids a circular\n   * dependency and lets the menu be tested in isolation.\n   */\n  constructor(onActionTriggered) {\n    // Record id of the row the menu is currently open FOR. Needed precisely\n    // because the menu is shared: the buttons are wired once at construction,\n    // so this field is how a click knows which contract it applies to.\n    this._currentRecordId = \"\";\n    // The three-dot trigger button the menu is currently open for. Tracked so its\n    // blue \"active\" highlight (pcf-dots-btn-active) can be cleared on close — the menu\n    // is shared, so without this the highlight would stick on a stale button.\n    this._activeTriggerButton = null;\n    this._onActionTriggered = onActionTriggered;\n    this._boundWindowClick = this.handleWindowClick.bind(this);\n    this.createDropdownMenu();\n    // Close dropdown when clicking outside\n    // Registered on window (not the grid container) because the menu sits\n    // on document.body — clicks anywhere on the page must dismiss it.\n    window.addEventListener(\"click\", this._boundWindowClick);\n  }\n  /**\n   * Builds the menu DOM exactly once. Items are created imperatively (not\n   * re-rendered per show) because their contents are static — only the\n   * comment badge visibility and the position change per invocation, both\n   * handled cheaply in show().\n   */\n  createDropdownMenu() {\n    this._dropdownElement = document.createElement(\"div\");\n    this._dropdownElement.className = \"pcf-dropdown\";\n    // 1. Edit Item\n    var btnEdit = document.createElement(\"button\");\n    btnEdit.className = \"pcf-dropdown-item\";\n    btnEdit.innerHTML = \"\\n            <svg viewBox=\\\"0 0 24 24\\\" fill=\\\"none\\\" stroke=\\\"currentColor\\\" stroke-width=\\\"1.6\\\" stroke-linecap=\\\"round\\\" stroke-linejoin=\\\"round\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                <path d=\\\"M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z\\\"/>\\n            </svg>\\n            Edit\\n        \";\n    btnEdit.onclick = () => {\n      this._onActionTriggered(\"Edit\", this._currentRecordId);\n      this.closeDropdown();\n    };\n    // 2. Comments Item\n    // Includes the #dropdown-comment-dot red badge (hidden by default):\n    // show() toggles it per row so users can see — before opening the\n    // dialog — that this contract already has comments worth reading.\n    var btnComment = document.createElement(\"button\");\n    btnComment.className = \"pcf-dropdown-item\";\n    btnComment.innerHTML = \"\\n            <svg viewBox=\\\"0 0 24 24\\\" fill=\\\"none\\\" stroke=\\\"currentColor\\\" stroke-width=\\\"1.6\\\" stroke-linecap=\\\"round\\\" stroke-linejoin=\\\"round\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                <path d=\\\"M4.5 5.25h15a1.5 1.5 0 0 1 1.5 1.5v8.5a1.5 1.5 0 0 1-1.5 1.5H11.5l-4 3.5v-3.5h-3a1.5 1.5 0 0 1-1.5-1.5v-8.5a1.5 1.5 0 0 1 1.5-1.5z\\\"/>\\n                <path d=\\\"M7.25 9.25h9.5\\\"/>\\n                <path d=\\\"M7.25 12.75h6.5\\\"/>\\n            </svg>\\n            Comments\\n            <span class=\\\"pcf-dot-inline\\\" id=\\\"dropdown-comment-dot\\\" style=\\\"display: none;\\\"></span>\\n        \";\n    btnComment.onclick = () => {\n      this._onActionTriggered(\"Comment\", this._currentRecordId);\n      this.closeDropdown();\n    };\n    // 3. Delete Item\n    var btnDelete = document.createElement(\"button\");\n    btnDelete.className = \"pcf-dropdown-item\";\n    btnDelete.innerHTML = \"\\n            <svg viewBox=\\\"0 0 24 24\\\" fill=\\\"none\\\" stroke=\\\"currentColor\\\" stroke-width=\\\"1.6\\\" stroke-linecap=\\\"round\\\" stroke-linejoin=\\\"round\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                <polyline points=\\\"3 6 5 6 21 6\\\"/>\\n                <path d=\\\"M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2\\\"/>\\n                <line x1=\\\"10\\\" y1=\\\"11\\\" x2=\\\"10\\\" y2=\\\"17\\\"/>\\n                <line x1=\\\"14\\\" y1=\\\"11\\\" x2=\\\"14\\\" y2=\\\"17\\\"/>\\n            </svg>\\n            Delete\\n        \";\n    btnDelete.onclick = () => {\n      this._onActionTriggered(\"Delete\", this._currentRecordId);\n      this.closeDropdown();\n    };\n    this._dropdownElement.appendChild(btnEdit);\n    this._dropdownElement.appendChild(btnComment);\n    this._dropdownElement.appendChild(btnDelete);\n    // Appending to body is best practice for custom absolute popups so they aren't clipped by 'overflow:hidden'\n    // Concretely: the grid scrolls inside an overflow container, so a\n    // menu parented inside it would be cut off at the container edge for\n    // rows near the bottom/right, and could land under neighbouring\n    // Canvas controls due to stacking contexts. Body-level positioning\n    // sidesteps both. The trade-off is manual cleanup in destroy().\n    document.body.appendChild(this._dropdownElement);\n  }\n  /**\n   * Opens (or repositions) the shared menu for one contract row.\n   * Called from the row's \"...\" button handler in GridRenderer.\n   *\n   * @param e           Click event of the trigger button — its currentTarget\n   *                    rect anchors the menu, so the menu follows whichever\n   *                    row was clicked even though the element is shared.\n   * @param recordId    Contract the menu now represents; stored so the\n   *                    statically-wired item buttons report the right row.\n   * @param hasComments Contract-level flag (ContractData.hasComments) that\n   *                    decides whether the red badge on \"Comments\" is shown.\n   */\n  show(e, recordId, hasComments) {\n    var target = e.currentTarget;\n    // Toggle: re-clicking the button that already owns the open menu closes it\n    // instead of reopening (the button's onclick stops propagation, so the window\n    // light-dismiss never fires for it — this is the sole toggle point).\n    if (this._activeTriggerButton === target && this._dropdownElement.classList.contains(\"open\")) {\n      this.closeDropdown();\n      return;\n    }\n    this._currentRecordId = recordId;\n    var rect = target.getBoundingClientRect();\n    // Give the clicked three-dot button the blue \"active\" highlight while its menu\n    // is open; clear it from any previously active button first (shared menu).\n    if (this._activeTriggerButton && this._activeTriggerButton !== target) {\n      this._activeTriggerButton.classList.remove(\"pcf-dots-btn-active\");\n    }\n    this._activeTriggerButton = target;\n    target.classList.add(\"pcf-dots-btn-active\");\n    // Show or hide comment dot inside dropdown\n    // Toggled on EVERY show() because the menu element is shared between\n    // rows — without this, the badge state of the previously opened row\n    // would leak onto the next one.\n    var dropdownDot = document.getElementById(\"dropdown-comment-dot\");\n    if (dropdownDot) {\n      dropdownDot.style.display = hasComments ? \"block\" : \"none\";\n    }\n    // Must be marked open BEFORE measuring: while closed the menu is not\n    // laid out, so offsetWidth/offsetHeight below would read 0 and the\n    // positioning math would be garbage.\n    this._dropdownElement.classList.add(\"open\");\n    var menuWidth = this._dropdownElement.offsetWidth;\n    var menuHeight = this._dropdownElement.offsetHeight;\n    var viewportPadding = 8;\n    // Horizontal: right-align the menu with the trigger button, but clamp\n    // into the viewport so rows near the screen edge don't spawn a\n    // half-visible (unclickable) menu.\n    var left = Math.max(viewportPadding, Math.min(rect.right - menuWidth, window.innerWidth - menuWidth - viewportPadding));\n    var preferredTop = rect.bottom + 4;\n    // Vertical: prefer opening below the button; if that would overflow\n    // the bottom of the viewport (rows at the end of a long grid), flip\n    // above it instead — same pattern native context menus use.\n    var top = preferredTop + menuHeight <= window.innerHeight - viewportPadding ? preferredTop : Math.max(viewportPadding, rect.top - menuHeight - 4);\n    this._dropdownElement.style.top = \"\".concat(top, \"px\");\n    this._dropdownElement.style.left = \"\".concat(left, \"px\");\n  }\n  /**\n   * Hides the menu via CSS class only — the element stays in the DOM so the\n   * next show() is just a class toggle + reposition, never a rebuild.\n   */\n  closeDropdown() {\n    this._dropdownElement.classList.remove(\"open\");\n    // Drop the blue highlight from the trigger button the menu was open for.\n    if (this._activeTriggerButton) {\n      this._activeTriggerButton.classList.remove(\"pcf-dots-btn-active\");\n      this._activeTriggerButton = null;\n    }\n  }\n  /**\n   * Window-level \"click outside closes the menu\" handler. It deliberately\n   * does NOT check whether the click landed inside the menu itself: the\n   * item buttons close the menu in their own handlers anyway, so closing\n   * unconditionally keeps this logic to a single exception (below).\n   */\n  handleWindowClick(e) {\n    // If the click is inside a dropdown trigger button, do nothing (let the button's handler manage it)\n    // Otherwise this global handler would close the menu in the same\n    // event cycle in which show() just opened it (the button click also\n    // bubbles to window), making the menu impossible to open.\n    var target = e.target;\n    if (target.closest('.pcf-dots-btn')) {\n      return;\n    }\n    this.closeDropdown();\n  }\n  /**\n   * Called from index.ts destroy(). Both cleanups are mandatory precisely\n   * because this class reaches outside the PCF container: the click\n   * listener lives on window and the menu element on document.body, so\n   * neither is torn down automatically when the framework disposes the\n   * control — skipping this leaks DOM nodes/listeners on every screen\n   * navigation.\n   */\n  destroy() {\n    window.removeEventListener(\"click\", this._boundWindowClick);\n    if (this._dropdownElement && this._dropdownElement.parentNode) {\n      this._dropdownElement.parentNode.removeChild(this._dropdownElement);\n    }\n  }\n}\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./TableConnectedToDataversePCF/ui/ActionDropdown.ts?\n}");

/***/ },

/***/ "./TableConnectedToDataversePCF/ui/GridRenderer.ts"
/*!*********************************************************!*\
  !*** ./TableConnectedToDataversePCF/ui/GridRenderer.ts ***!
  \*********************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

eval("{__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   GridRenderer: () => (/* binding */ GridRenderer)\n/* harmony export */ });\n/* harmony import */ var _services_DatasetParser__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../services/DatasetParser */ \"./TableConnectedToDataversePCF/services/DatasetParser.ts\");\n\n/**\n * Handles the DOM manipulation and layout construction of the main hierarchical grid.\n */\nclass GridRenderer {\n  /**\n   * @param container        Root div handed over by index.ts (the PCF container).\n   * @param expandedRowIds   Shared (by reference) expand-state set owned by index.ts\n   *                          (holds EXPANDED ids; empty = everything collapsed).\n   * @param actionDropdown   Pre-built three-dot menu component for contract rows.\n   * @param onActionTriggered Single callback through which EVERY user action reaches\n   *                          index.ts — keeping this the only exit point is what\n   *                          guarantees the \"no Dataverse calls from the renderer\" rule.\n   */\n  constructor(container, expandedRowIds, actionDropdown, onActionTriggered) {\n    // Last ShowPlannedCost value, so the ResizeObserver can re-fit columns (which needs\n    // the column count) without a fresh context. Set on every render().\n    this._lastShowPlanned = false;\n    // Guards against the ResizeObserver re-entering while render() is mid-flight (setting\n    // the wrapper height itself triggers a resize notification).\n    this._isRendering = false;\n    // Re-fits the columns whenever the host resizes the control, so the internal\n    // horizontal scrollbar never lingers after a width change (see fitColumnsToScrollViewport).\n    this._resizeObserver = null;\n    // The add/month dropdowns and the floating comment tooltip are appended to\n    // document.body (not the table) so the scroll container's overflow can't clip\n    // them. These references exist purely so render()/destroy() can tear them down;\n    // leaking them would leave orphaned popups floating over the Canvas app.\n    this._addDropdownElement = null;\n    // The \"Add\" menu button the dropdown is currently open for, so its blue \"active\"\n    // highlight can be cleared on close and re-clicks can toggle it shut.\n    this._activeAddMenuButton = null;\n    this._monthDropdownElement = null;\n    // Month cell currently highlighted while its dropdown is open, so the \"active\"\n    // class can be removed on close.\n    this._activeMonthCell = null;\n    this._commentTooltipElement = null;\n    this._container = container;\n    this._expandedRowIds = expandedRowIds;\n    this._actionDropdown = actionDropdown;\n    this._onActionTriggered = onActionTriggered;\n    this._boundAddDropdownWindowClick = this.handleAddDropdownWindowClick.bind(this);\n    this._boundMonthDropdownWindowClick = this.handleMonthDropdownWindowClick.bind(this);\n    this.initDOM();\n  }\n  /**\n   * Builds the static DOM skeleton once (wrapper > table > thead/tbody). Only the\n   * contents of thead/tbody are rewritten on each render(); keeping the skeleton\n   * stable preserves the wrapper's horizontal/vertical scroll position across the\n   * frequent Canvas-triggered re-renders.\n   */\n  initDOM() {\n    var mainContainer = document.createElement(\"div\");\n    mainContainer.className = \"pcf-budget-grid\";\n    // Table Wrapper for horizontal scrolling\n    this._tableWrapper = document.createElement(\"div\");\n    this._tableWrapper.className = \"pcf-table-scroll\";\n    this._table = document.createElement(\"table\");\n    this._table.className = \"pcf-table\";\n    this._thead = document.createElement(\"thead\");\n    this._tbody = document.createElement(\"tbody\");\n    this._table.appendChild(this._thead);\n    this._table.appendChild(this._tbody);\n    this._tableWrapper.appendChild(this._table);\n    mainContainer.appendChild(this._tableWrapper);\n    this._container.appendChild(mainContainer);\n    // Re-fit the columns whenever the host changes the control's size. render() only\n    // fits once, against the width at render time; a later Canvas relayout (sidebar\n    // toggle, window resize, the vertical scrollbar appearing after async layout)\n    // could otherwise leave a stale ~15px horizontal scrollbar. The _isRendering\n    // guard avoids re-entrancy from the height we set during render(); the rAF\n    // coalesces bursts and defers measurement until layout has settled.\n    if (typeof ResizeObserver !== \"undefined\") {\n      this._resizeObserver = new ResizeObserver(() => {\n        if (this._isRendering || this._tbody.children.length === 0) {\n          return;\n        }\n        window.requestAnimationFrame(() => {\n          if (this._isRendering || this._tbody.children.length === 0) {\n            return;\n          }\n          this.fitColumnsToScrollViewport(this._lastShowPlanned);\n        });\n      });\n      this._resizeObserver.observe(this._tableWrapper);\n    }\n  }\n  /**\n   * Full re-render entry point, called by index.ts on every updateView and on\n   * PCF_RE_RENDER. Flow: tear down floating UI -> loading guard -> parse dataset to\n   * tree (fallback to sample data when empty) -> apply UI-only filters\n   * (CostPaidByFilter, ShowEmptyAccounts) -> attach standard contract options ->\n   * size columns -> headers (cluster timeline + column row) -> body rows\n   * (account/subaccount/contract/add) -> grand total + sticky-footer spacer.\n   *\n   * Grand totals are accumulated over the FILTERED tree (so the footer matches the\n   * visible rows) but include collapsed accounts — collapsing hides detail, it must\n   * not change totals.\n   */\n  render(context) {\n    var _a, _b, _c, _d, _e;\n    // Suppress the ResizeObserver re-fit while we synchronously rebuild and resize\n    // the grid; it runs again (via rAF) once this render settles.\n    this._isRendering = true;\n    // Dropdowns/tooltips live on document.body, so a re-render would otherwise\n    // orphan them (their anchor cells are about to be destroyed).\n    this.closeMonthDropdown();\n    this.closeAddDropdown();\n    this.hideCommentTooltip();\n    var dataset = context.parameters.DevexCapexCostSummary;\n    // Canvas datasets arrive asynchronously; rendering before load completes would\n    // briefly flash the sample fallback data, so bail out with a loading message.\n    if (dataset.loading) {\n      this.renderLoading();\n      this._isRendering = false;\n      return;\n    }\n    var accounts = _services_DatasetParser__WEBPACK_IMPORTED_MODULE_0__.DatasetParser.parseDatasetToTree(dataset);\n    var showPlanned = ((_a = context.parameters.ShowPlannedCost) === null || _a === void 0 ? void 0 : _a.raw) || false;\n    // Remembered for the ResizeObserver's column re-fit (it has no context).\n    this._lastShowPlanned = showPlanned;\n    // Defaults to true: hiding accounts is the opt-in behaviour.\n    var showEmptyAccounts = (_c = (_b = context.parameters.ShowEmptyAccounts) === null || _b === void 0 ? void 0 : _b.raw) !== null && _c !== void 0 ? _c : true;\n    var costPaidByFilter = (((_d = context.parameters.CostPaidByFilter) === null || _d === void 0 ? void 0 : _d.raw) || \"Show All Cost\").toString().trim();\n    // StartYear drives which year's M1..M12 Canvas put into the collection; here it\n    // is only used for the SetCostPaidUnpaid payload (month headers no longer show\n    // the year — Bug 13304).\n    var startYear = ((_e = context.parameters.StartYear) === null || _e === void 0 ? void 0 : _e.raw) || 2026;\n    // Both filters are UI-only: Canvas data is untouched, and subaccount/account\n    // totals are recalculated from the rows that remain visible (business rule for\n    // the DevCo/SPV filter).\n    accounts = this.applyCostPaidByFilter(accounts, costPaidByFilter);\n    if (!showEmptyAccounts) {\n      accounts = this.removeEmptyAccounts(accounts);\n    }\n    // Second dataset (colStandardContractOptionsForPCF): per-subaccount list of\n    // standard contracts offered by the Add menu. Applied AFTER the filters so the\n    // options land on the cloned subaccount objects the filters produced.\n    var standardContractOptionsBySubaccount = _services_DatasetParser__WEBPACK_IMPORTED_MODULE_0__.DatasetParser.parseStandardContractOptionsDataset(context.parameters.StandardContractOptions);\n    this.applyStandardContractOptions(accounts, standardContractOptionsBySubaccount);\n    // 3 preceding columns (Number, Name, Total Costs) + 2 optional (Planned, Actual)\n    var precedingColSpan = 3 + (showPlanned ? 2 : 0);\n    // Canvas reports the allocated size; fall back to the live container size when\n    // the host returns 0 (e.g. first render in the test harness).\n    var availableWidth = Math.floor(context.mode.allocatedWidth || this._container.clientWidth || 0);\n    var availableHeight = Math.floor(context.mode.allocatedHeight || this._container.clientHeight || 0);\n    this.applyViewportHeight(availableHeight);\n    this.renderColGroup(showPlanned, availableWidth);\n    this.renderHeaders(context, showPlanned, precedingColSpan);\n    this._tbody.innerHTML = \"\";\n    if (accounts.length === 0) {\n      this.renderEmptyState(precedingColSpan + 13);\n      this._isRendering = false;\n      return;\n    }\n    // Grand totals sum the account-level TotalCost/PlannedCost/ActualCost fields\n    // (Canvas-computed all-years values, or filter-recalculated copies) — NOT a sum\n    // of M1..M12, which only cover the selected year.\n    var grandTotalCost = 0;\n    var grandPlannedCost = 0;\n    var grandActualCost = 0;\n    var grandMonthlyTotals = Array(12).fill(0);\n    var totalColSpan = precedingColSpan + 12 + 1;\n    // Visual gap rows only make sense between an account that actually showed\n    // detail rows and the next account; this flag tracks that across iterations.\n    var previousAccountRenderedDetails = false;\n    // Iterate Accounts (Level 0)\n    accounts.forEach((acc, accountIndex) => {\n      if (accountIndex > 0 && previousAccountRenderedDetails) {\n        this._tbody.appendChild(this.createGroupGapRow(totalColSpan));\n      }\n      var isAccCollapsed = !this._expandedRowIds.has(acc.id);\n      grandTotalCost += acc.totalCost;\n      grandPlannedCost += acc.plannedCost;\n      grandActualCost += acc.actualCost;\n      acc.months.forEach((val, idx) => {\n        grandMonthlyTotals[idx] += val;\n      });\n      this._tbody.appendChild(this.createAccountRow(acc, isAccCollapsed, showPlanned));\n      // Iterate Subaccounts (Level 1) if expanded\n      if (!isAccCollapsed) {\n        acc.subaccounts.forEach((sub, subIndex) => {\n          var isSubCollapsed = !this._expandedRowIds.has(sub.id);\n          var isLastSubaccount = subIndex === acc.subaccounts.length - 1;\n          this._tbody.appendChild(this.createSubaccountRow(sub, isSubCollapsed, showPlanned, isLastSubaccount, subIndex === 0));\n          // Iterate Contracts (Level 2) and Add Button if expanded\n          // The isLast*/index===0 flags feed the tree connector classes —\n          // they decide between T-branch, L-branch and pass-through lines.\n          // Getting them wrong reintroduces the old \"line bleeding into the\n          // neighbouring row\" regressions.\n          if (!isSubCollapsed) {\n            sub.contracts.forEach((contract, contractIndex) => {\n              var isLastContract = contractIndex === sub.contracts.length - 1;\n              this._tbody.appendChild(this.createContractRow(contract, showPlanned, isLastSubaccount, contractIndex === 0, isLastContract, startYear));\n            });\n            // Every expanded subaccount ends with an \"Add\" row, even when it\n            // has no contracts yet — that is the only entry point for adding.\n            this._tbody.appendChild(this.createAddRow(sub, showPlanned, isLastSubaccount));\n          }\n        });\n      }\n      previousAccountRenderedDetails = !isAccCollapsed && acc.subaccounts.length > 0;\n    });\n    this.renderGrandTotal(grandTotalCost, grandPlannedCost, grandActualCost, grandMonthlyTotals, showPlanned);\n    // Re-fit the columns now that all rows are in the DOM: if a vertical scrollbar\n    // appeared it has shrunk the wrapper's usable width, and the first\n    // renderColGroup (sized off allocatedWidth) would now overflow by the scrollbar\n    // width, showing a spurious horizontal scrollbar. See the method JSDoc.\n    this.fitColumnsToScrollViewport(showPlanned);\n    this.syncGrandTotalSpacer(totalColSpan);\n    this._isRendering = false;\n  }\n  /**\n   * Second column-sizing pass, run AFTER the rows exist so the vertical scrollbar's\n   * presence is known. renderColGroup's first pass sizes the table to the Canvas\n   * allocatedWidth, but once enough rows force a vertical scrollbar that scrollbar\n   * eats ~15-17px of the scroll wrapper's inner width — leaving the table wider than\n   * the viewport and producing an UNNECESSARY horizontal scrollbar (Bug 13125).\n   *\n   * Re-running renderColGroup against the wrapper's live clientWidth (which already\n   * excludes the vertical scrollbar) makes the table fit exactly. The\n   * Math.max(minimumTableWidth, ...) inside renderColGroup still preserves the\n   * INTENDED horizontal scroll when the canvas is genuinely too narrow\n   * (minimumTableWidth > clientWidth), so this only removes the spurious case.\n   *\n   * No oscillation risk: shrinking the table never removes the vertical scrollbar\n   * (rows still overflow vertically), so clientWidth stays stable on the second pass.\n   */\n  fitColumnsToScrollViewport(showPlanned) {\n    var innerWidth = this._tableWrapper.clientWidth;\n    // 0 in the test harness / before first layout — leave the allocatedWidth-based\n    // sizing from the first pass untouched.\n    if (innerWidth <= 0) {\n      return;\n    }\n    this.renderColGroup(showPlanned, innerWidth);\n  }\n  /**\n   * Pins the scroll wrapper to the exact height Canvas allocated. Without an explicit\n   * height the wrapper grows with content, which kills both the sticky header and the\n   * sticky grand-total row (position:sticky needs a bounded scroll container).\n   * Height 0/unknown (test harness, first paint) clears the constraint instead of\n   * collapsing the grid to nothing.\n   */\n  applyViewportHeight(availableHeight) {\n    if (availableHeight > 0) {\n      this._tableWrapper.style.height = \"\".concat(availableHeight, \"px\");\n      this._tableWrapper.style.maxHeight = \"\".concat(availableHeight, \"px\");\n    } else {\n      this._tableWrapper.style.height = \"\";\n      this._tableWrapper.style.maxHeight = \"\";\n    }\n  }\n  /**\n   * Keeps the grand-total row visually glued to the BOTTOM of the viewport even when\n   * there are too few body rows to fill it: inserts a filler row sized to the leftover\n   * space just above the grand total. Must run after the rows are in the DOM because\n   * it measures real offsetHeights; fragile if row heights later become dynamic\n   * (e.g. wrapping text) since the measurement happens only once per render.\n   */\n  syncGrandTotalSpacer(totalColSpan) {\n    var existingSpacer = this._tbody.querySelector(\".pcf-row-footer-spacer\");\n    if (existingSpacer) {\n      existingSpacer.remove();\n    }\n    var grandRow = this._tbody.querySelector(\".pcf-row-grand\");\n    if (!grandRow) {\n      return;\n    }\n    var wrapperHeight = this._tableWrapper.clientHeight;\n    if (wrapperHeight <= 0) {\n      return;\n    }\n    var headerHeight = this._thead.offsetHeight;\n    var grandHeight = grandRow.offsetHeight;\n    var bodyRows = Array.from(this._tbody.children).filter(row => row !== grandRow);\n    var renderedBodyHeight = bodyRows.reduce((total, row) => total + row.offsetHeight, 0);\n    var availableBodyHeight = wrapperHeight - headerHeight - grandHeight;\n    var spacerHeight = availableBodyHeight - renderedBodyHeight;\n    if (spacerHeight <= 0) {\n      return;\n    }\n    var spacerRow = document.createElement(\"tr\");\n    spacerRow.className = \"pcf-row-footer-spacer\";\n    var spacerCell = document.createElement(\"td\");\n    spacerCell.colSpan = totalColSpan;\n    spacerCell.style.height = \"\".concat(spacerHeight, \"px\");\n    spacerRow.appendChild(spacerCell);\n    this._tbody.insertBefore(spacerRow, grandRow);\n  }\n  /**\n   * Attaches each subaccount's standard contract options (from the\n   * StandardContractOptions dataset) so showAddDropdown can decide between\n   * no item / direct action / submenu. Mutates the (already cloned/filtered)\n   * tree in place — safe because the tree is rebuilt every render.\n   */\n  applyStandardContractOptions(accounts, optionsBySubaccount) {\n    if (optionsBySubaccount.size === 0) {\n      return;\n    }\n    accounts.forEach(account => {\n      account.subaccounts.forEach(subaccount => {\n        subaccount.standardContractOptions = optionsBySubaccount.get(subaccount.id) || [];\n      });\n    });\n  }\n  /**\n   * UI-only DevCo/SPV filter (CostPaidByFilter input). Contracts that don't match are\n   * dropped from the rendered tree and subaccount/account totals are recalculated\n   * from the surviving contracts so the visible numbers stay self-consistent — the\n   * Canvas collection itself is never modified. Unrecognised filter values fail open\n   * (show everything) so a typo in the Canvas dropdown can't blank the grid.\n   */\n  applyCostPaidByFilter(accounts, filterValue) {\n    var normalizedFilter = filterValue.trim().toLowerCase();\n    // Accept the historical \"Show All Cost\" label plus shorter variants Canvas has\n    // used over time.\n    if (!normalizedFilter || normalizedFilter === \"show all cost\" || normalizedFilter === \"show all\" || normalizedFilter === \"all\") {\n      return accounts;\n    }\n    var expectedCostPaidBy = normalizedFilter.includes(\"devco\") ? \"devco\" : normalizedFilter.includes(\"spv\") ? \"spv\" : \"\";\n    if (!expectedCostPaidBy) {\n      return accounts;\n    }\n    return accounts.map(account => this.recalculateAccount(Object.assign(Object.assign({}, account), {\n      subaccounts: account.subaccounts.map(subaccount => this.recalculateSubaccount(Object.assign(Object.assign({}, subaccount), {\n        contracts: subaccount.contracts.filter(contract => this.contractMatchesCostPaidBy(contract, expectedCostPaidBy))\n      })))\n    })));\n  }\n  /**\n   * Matches against BOTH CostPaidBy and SubLabel because older rows only carried the\n   * \"[SPV]\"/\"[DEVCO]\" tag inside SubLabel; substring matching keeps both data shapes\n   * working without a Canvas migration.\n   */\n  contractMatchesCostPaidBy(contract, expectedCostPaidBy) {\n    var source = \"\".concat(contract.costPaidBy || \"\", \" \").concat(contract.subLabel || \"\").toLowerCase();\n    return source.includes(expectedCostPaidBy);\n  }\n  /**\n   * Implements ShowEmptyAccounts=false: drops subaccounts with no contracts AND no\n   * cost anywhere, then drops accounts left with no subaccounts and no own cost.\n   * \"Empty\" deliberately checks all cost fields (hasAnyCost) so an account with\n   * all-years totals but nothing in the selected year is still shown.\n   */\n  removeEmptyAccounts(accounts) {\n    return accounts.map(account => {\n      var subaccounts = account.subaccounts.filter(subaccount => subaccount.contracts.length > 0 || this.hasAnyCost(subaccount));\n      if (subaccounts.length === 0) {\n        return Object.assign(Object.assign({}, account), {\n          subaccounts\n        });\n      }\n      return this.recalculateAccount(Object.assign(Object.assign({}, account), {\n        subaccounts\n      }));\n    }).filter(account => account.subaccounts.length > 0 || this.hasAnyCost(account));\n  }\n  /** \"Has any cost\" across all-years totals AND the selected-year months — used by\n   *  removeEmptyAccounts so rows with off-year costs are never hidden by mistake. */\n  hasAnyCost(row) {\n    return row.totalCost !== 0 || row.plannedCost !== 0 || row.actualCost !== 0 || row.months.some(monthCost => monthCost !== 0);\n  }\n  /**\n   * Rebuilds an account's totals from its (possibly filtered) subaccounts. ONLY used\n   * after UI-side filtering — in the unfiltered path the Canvas-computed all-years\n   * totals are displayed untouched. Summing child totalCost fields (not M1..M12)\n   * keeps the \"never derive all-year totals from months\" rule intact.\n   */\n  recalculateAccount(account) {\n    if (account.subaccounts.length === 0) {\n      return Object.assign(Object.assign({}, account), {\n        totalCost: 0,\n        plannedCost: 0,\n        actualCost: 0,\n        months: Array(12).fill(0)\n      });\n    }\n    return Object.assign(Object.assign({}, account), {\n      totalCost: account.subaccounts.reduce((total, subaccount) => total + subaccount.totalCost, 0),\n      plannedCost: account.subaccounts.reduce((total, subaccount) => total + subaccount.plannedCost, 0),\n      actualCost: account.subaccounts.reduce((total, subaccount) => total + subaccount.actualCost, 0),\n      months: this.sumMonths(account.subaccounts)\n    });\n  }\n  /** Subaccount counterpart of recalculateAccount — totals become the sum of the\n   *  visible contracts so the DevCo/SPV filter shows internally consistent numbers. */\n  recalculateSubaccount(subaccount) {\n    if (subaccount.contracts.length === 0) {\n      return Object.assign(Object.assign({}, subaccount), {\n        totalCost: 0,\n        plannedCost: 0,\n        actualCost: 0,\n        months: Array(12).fill(0)\n      });\n    }\n    return Object.assign(Object.assign({}, subaccount), {\n      totalCost: subaccount.contracts.reduce((total, contract) => total + contract.totalCost, 0),\n      plannedCost: subaccount.contracts.reduce((total, contract) => total + contract.plannedCost, 0),\n      actualCost: subaccount.contracts.reduce((total, contract) => total + contract.actualCost, 0),\n      months: this.sumMonths(subaccount.contracts)\n    });\n  }\n  /** Element-wise sum of the 12 selected-year month columns (this is fine — months\n   *  may be summed into month totals; only all-years totals must not come from here). */\n  sumMonths(rows) {\n    var totals = Array(12).fill(0);\n    rows.forEach(row => {\n      row.months.forEach((monthCost, monthIndex) => {\n        totals[monthIndex] += monthCost;\n      });\n    });\n    return totals;\n  }\n  /**\n   * FRAGILE: single source of truth for column sizing. Fixed-width leading/trailing\n   * columns + 12 EQUAL month columns that absorb any extra space. The table gets an\n   * explicit width (and min-width) so that when the columns' minimums exceed the\n   * Canvas allocation the wrapper scrolls horizontally instead of letting the browser\n   * squeeze columns. The --pcf-month-width CSS variable must match the computed month\n   * width — CSS (cluster timeline ticks/pills) positions against it.\n   */\n  renderColGroup(showPlanned, availableWidth) {\n    var existingColGroup = this._table.querySelector(\"colgroup\");\n    if (existingColGroup) {\n      existingColGroup.remove();\n    }\n    var widths = [GridRenderer.NUMBER_COLUMN_MIN_WIDTH, GridRenderer.ACCOUNT_NAME_COLUMN_MIN_WIDTH, GridRenderer.TOTAL_COST_COLUMN_MIN_WIDTH];\n    if (showPlanned) {\n      widths.push(GridRenderer.PLANNED_COST_COLUMN_MIN_WIDTH, GridRenderer.ACTUAL_COST_COLUMN_MIN_WIDTH);\n    }\n    // Grow-don't-squeeze policy: take whichever is larger, the minimum table width\n    // or the Canvas allocation, then split the leftover evenly across the 12 month\n    // columns. Math.floor + recomputing tableWidth from the floored month width\n    // avoids sub-pixel rounding drift that previously misaligned the cluster\n    // timeline ticks against the month column borders.\n    var fixedWidth = widths.reduce((total, width) => total + width, 0) + GridRenderer.ACTION_COLUMN_WIDTH;\n    var monthMinimumTotal = GridRenderer.MONTH_COLUMN_MIN_WIDTH * 12;\n    var minimumTableWidth = fixedWidth + monthMinimumTotal;\n    var targetTableWidth = Math.max(minimumTableWidth, availableWidth);\n    var monthWidth = Math.floor((targetTableWidth - fixedWidth) / 12);\n    var tableWidth = fixedWidth + monthWidth * 12;\n    widths.push(...Array(12).fill(monthWidth), GridRenderer.ACTION_COLUMN_WIDTH);\n    var colGroup = document.createElement(\"colgroup\");\n    widths.forEach(width => {\n      var col = document.createElement(\"col\");\n      col.style.width = \"\".concat(width, \"px\");\n      colGroup.appendChild(col);\n    });\n    this._table.style.width = \"\".concat(tableWidth, \"px\");\n    this._table.style.minWidth = \"\".concat(tableWidth, \"px\");\n    this._table.style.setProperty(\"--pcf-month-width\", \"\".concat(monthWidth, \"px\"));\n    this._table.insertBefore(colGroup, this._thead);\n  }\n  /**\n   * Builds the two sticky header rows:\n   *  Row 1 — cluster timeline: pills (\"Cluster N\") spanning the month columns covered\n   *          by each cluster, driven by the Cluster1Date..Cluster6Date inputs via\n   *          DatasetParser.calculateClusterTimeline. The timeline deliberately starts\n   *          at the FIRST MONTH column, never above Total Costs/Planned/Actual — the\n   *          leading th with colspan=precedingColSpan guarantees that offset.\n   *  Row 2 — standard column captions, including the per-month three-letter labels\n   *          (Jan..Dec, no year — Bug 13304) and the boundary tick classes that draw\n   *          the cluster start lines.\n   */\n  renderHeaders(context, showPlanned, precedingColSpan) {\n    this._thead.innerHTML = \"\";\n    // --- Row 1: Cluster Timeline ---\n    var timelineRow = document.createElement(\"tr\");\n    timelineRow.className = \"pcf-timeline-row\";\n    var leadingTh = document.createElement(\"th\");\n    leadingTh.setAttribute(\"colspan\", String(precedingColSpan));\n    leadingTh.className = \"pcf-timeline-empty\";\n    timelineRow.appendChild(leadingTh);\n    var clusterTimeline = _services_DatasetParser__WEBPACK_IMPORTED_MODULE_0__.DatasetParser.calculateClusterTimeline(context);\n    var clusterSpans = clusterTimeline.spans;\n    clusterSpans.forEach(span => {\n      var th = document.createElement(\"th\");\n      th.setAttribute(\"colspan\", String(span.length));\n      th.className = \"pcf-timeline-cell\";\n      // clusterIndex 0 = months before the first cluster: they still need a\n      // spanning th (to keep column alignment) but get no pill.\n      if (span.clusterIndex > 0) {\n        var pillContainer = document.createElement(\"div\");\n        pillContainer.className = \"pcf-cluster-pill-container\";\n        var pill = document.createElement(\"div\");\n        pill.className = \"pcf-cluster-pill\";\n        pill.innerText = \"Cluster \".concat(span.clusterIndex);\n        pillContainer.appendChild(pill);\n        th.appendChild(pillContainer);\n      }\n      timelineRow.appendChild(th);\n    });\n    // 1 trailing column for the action dots\n    var trailingTh = document.createElement(\"th\");\n    trailingTh.setAttribute(\"colspan\", \"1\");\n    trailingTh.className = \"pcf-timeline-empty\";\n    timelineRow.appendChild(trailingTh);\n    this._thead.appendChild(timelineRow);\n    // --- Row 2: Standard Columns ---\n    var colHeaderRow = document.createElement(\"tr\");\n    colHeaderRow.className = \"pcf-header-row\";\n    var thNumber = document.createElement(\"th\");\n    thNumber.innerText = \"Number\";\n    thNumber.className = \"pcf-header-number\";\n    colHeaderRow.appendChild(thNumber);\n    var thName = document.createElement(\"th\");\n    thName.innerText = \"Account Name\";\n    thName.className = \"pcf-header-name\";\n    colHeaderRow.appendChild(thName);\n    var thTotal = document.createElement(\"th\");\n    thTotal.innerText = \"Total Costs\";\n    thTotal.className = \"pcf-col-num pcf-col-cost\";\n    colHeaderRow.appendChild(thTotal);\n    if (showPlanned) {\n      // Column order: \"Total Paid\" is shown BEFORE \"Total Planned\".\n      var thActual = document.createElement(\"th\");\n      thActual.innerText = \"Total Paid\";\n      thActual.className = \"pcf-col-num pcf-col-cost\";\n      colHeaderRow.appendChild(thActual);\n      var thPlanned = document.createElement(\"th\");\n      // Bug 13280: PBI 13117 names these columns \"Total Planned\" / \"Total Paid\"\n      // (not \"...Cost\" / \"...Actual Cost\").\n      thPlanned.innerText = \"Total Planned\";\n      thPlanned.className = \"pcf-col-num pcf-col-cost\";\n      colHeaderRow.appendChild(thPlanned);\n    }\n    // A month is a \"cluster start\" when its cluster index differs from the previous\n    // month's — those columns get the vertical boundary tick that visually separates\n    // clusters in the header. Month 0 is excluded on purpose: the timeline's own\n    // start marker (pcf-col-timeline-start) covers it.\n    var monthClusterStarts = new Set();\n    for (var monthIndex = 1; monthIndex < clusterTimeline.monthClusters.length; monthIndex++) {\n      if (clusterTimeline.monthClusters[monthIndex] !== clusterTimeline.monthClusters[monthIndex - 1]) {\n        monthClusterStarts.add(monthIndex);\n      }\n    }\n    // 12 Months\n    for (var i = 1; i <= 12; i++) {\n      var thM = document.createElement(\"th\");\n      // Bug 13304: three-letter month abbreviation, no year (the selected year is\n      // already shown in the year picker above the grid).\n      thM.innerText = GridRenderer.MONTH_ABBREVIATIONS[i - 1];\n      thM.className = \"pcf-col-num pcf-col-month-header\";\n      // i is 1-based for the caption; monthClusterStarts is 0-based, hence i - 1.\n      if (monthClusterStarts.has(i - 1)) {\n        thM.classList.add(\"pcf-col-month-cluster-start\");\n      }\n      if (i === 12) {\n        thM.classList.add(\"pcf-col-timeline-end\");\n      }\n      if (i === 1) {\n        thM.classList.add(\"pcf-col-timeline-start\");\n        // The horizontal timeline line is one absolutely-positioned span\n        // anchored in the FIRST month header and stretched across all 12 via\n        // CSS (--pcf-month-width). Anchoring it here, not in the Total Costs\n        // column, is what keeps the line from starting too far left.\n        var timelineTrack = document.createElement(\"span\");\n        timelineTrack.className = \"pcf-timeline-track\";\n        thM.appendChild(timelineTrack);\n      }\n      colHeaderRow.appendChild(thM);\n    }\n    var thActions = document.createElement(\"th\");\n    colHeaderRow.appendChild(thActions);\n    this._thead.appendChild(colHeaderRow);\n  }\n  /**\n   * Level-0 row. Accounts are aggregate-only: no tree connector lines (they are the\n   * roots), no action menu, and their numeric cells are read-only rollups. The\n   * +/− toggle only appears when there are subaccounts; otherwise a spacer keeps the\n   * number text aligned with toggled rows (a missing spacer was the cause of the old\n   * \"half-filled number column\" look).\n   */\n  createAccountRow(acc, isCollapsed, showPlanned) {\n    var tr = document.createElement(\"tr\");\n    tr.className = \"pcf-row-account\";\n    var tdNumber = document.createElement(\"td\");\n    tdNumber.className = \"pcf-cell-tree pcf-cell-number pcf-cell-tree-account\";\n    if (acc.subaccounts.length > 0) {\n      var btnToggle = document.createElement(\"button\");\n      btnToggle.className = \"pcf-toggle\".concat(isCollapsed ? \"\" : \" pcf-toggle-expanded\");\n      btnToggle.innerText = isCollapsed ? \"+\" : \"−\";\n      btnToggle.onclick = () => this.toggleCollapse(acc.id);\n      tdNumber.appendChild(btnToggle);\n    } else {\n      var leafSpacer = document.createElement(\"span\");\n      leafSpacer.className = \"pcf-leaf-spacer\";\n      tdNumber.appendChild(leafSpacer);\n    }\n    var numText = document.createTextNode(acc.number);\n    tdNumber.appendChild(numText);\n    tr.appendChild(tdNumber);\n    var tdName = document.createElement(\"td\");\n    tdName.className = \"pcf-cell-name pcf-cell-name-account\";\n    tdName.innerText = acc.name;\n    tdName.title = acc.name;\n    tr.appendChild(tdName);\n    this.appendNumericCell(tr, acc.totalCost, \"pcf-cell-total pcf-col-cost\");\n    if (showPlanned) {\n      this.appendNumericCell(tr, acc.actualCost, \"pcf-cell-total pcf-col-cost\");\n      this.appendNumericCell(tr, acc.plannedCost, \"pcf-cell-total pcf-col-cost\");\n    }\n    acc.months.forEach(val => this.appendNumericCell(tr, val, \"pcf-cell-month\"));\n    var tdActions = document.createElement(\"td\");\n    tdActions.className = \"pcf-cell-actions\";\n    tr.appendChild(tdActions);\n    return tr;\n  }\n  /**\n   * Level-1 row. FRAGILE tree connector logic:\n   * - isLastSubaccount  => \"L\" branch (line stops here) vs \"T\" branch (line continues\n   *   down to the next sibling). Wrong choice makes the vertical line either stop\n   *   early or run past the last subaccount into the following account block.\n   * - connectsFromAccountToggle (true only for the FIRST subaccount) adds\n   *   pcf-tree-from-toggle, which shortens the connector's top so it meets the parent\n   *   account's toggle button instead of intruding into the parent row above —\n   *   removing it reintroduces that past regression.\n   */\n  createSubaccountRow(sub, isCollapsed, showPlanned, isLastSubaccount, connectsFromAccountToggle) {\n    var tr = document.createElement(\"tr\");\n    tr.className = \"pcf-row-sub\";\n    var tdNumber = document.createElement(\"td\");\n    tdNumber.className = \"pcf-cell-tree pcf-cell-number pcf-cell-tree-sub\";\n    // \"-toggle\" branch variants align the horizontal stub with this row's own\n    // toggle button (subaccounts always have one), unlike the plain contract\n    // branches further down.\n    var vLine = document.createElement(\"span\");\n    var branchClass = isLastSubaccount ? \"pcf-tree-l-toggle\" : \"pcf-tree-t-toggle\";\n    var toggleConnectorClass = connectsFromAccountToggle ? \" pcf-tree-from-toggle\" : \"\";\n    vLine.className = \"pcf-tree-spacer \".concat(branchClass).concat(toggleConnectorClass);\n    tdNumber.appendChild(vLine);\n    var btnToggle = document.createElement(\"button\");\n    btnToggle.className = \"pcf-toggle\".concat(isCollapsed ? \"\" : \" pcf-toggle-expanded\");\n    btnToggle.innerText = isCollapsed ? \"+\" : \"−\";\n    btnToggle.onclick = () => this.toggleCollapse(sub.id);\n    tdNumber.appendChild(btnToggle);\n    var numText = document.createTextNode(sub.number);\n    tdNumber.appendChild(numText);\n    tr.appendChild(tdNumber);\n    var tdName = document.createElement(\"td\");\n    tdName.className = \"pcf-cell-name pcf-cell-name-sub\";\n    tdName.innerText = sub.name;\n    tdName.title = sub.name;\n    tr.appendChild(tdName);\n    this.appendNumericCell(tr, sub.totalCost, \"pcf-cell-total pcf-col-cost\");\n    if (showPlanned) {\n      this.appendNumericCell(tr, sub.actualCost, \"pcf-cell-total pcf-col-cost\");\n      this.appendNumericCell(tr, sub.plannedCost, \"pcf-cell-total pcf-col-cost\");\n    }\n    sub.months.forEach(val => this.appendNumericCell(tr, val, \"pcf-cell-month\"));\n    var tdActions = document.createElement(\"td\");\n    tdActions.className = \"pcf-cell-actions\";\n    tr.appendChild(tdActions);\n    return tr;\n  }\n  /**\n   * Level-2 (leaf) row — the only row type with user actions: three-dot menu\n   * (ActionDropdown), clickable month cells (paid/unpaid + comments) and comment\n   * indicators.\n   *\n   * FRAGILE tree connectors — two stacked spans in the number cell:\n   * - line1: a plain vertical pass-through drawn ONLY while the parent subaccount is\n   *   not the last one (the account-level line must keep running behind this row to\n   *   reach later subaccount siblings).\n   * - line2: this contract's own branch — \"L\" for the last contract, \"T\" otherwise;\n   *   pcf-tree-from-toggle on the first contract trims the top so the line meets the\n   *   subaccount's toggle rather than poking into the row above (past regression).\n   *\n   * pcf-row-standard turns the row text blue for IsStandardContract rows.\n   */\n  createContractRow(contract, showPlanned, isLastSubaccount, connectsFromSubaccountToggle, isLastContract, startYear) {\n    var _a, _b;\n    var tr = document.createElement(\"tr\");\n    tr.className = \"pcf-row-contract\".concat(contract.isStandardContract ? \" pcf-row-standard\" : \"\");\n    // BUG 13888: action-menu indicators must represent ANY comment attached to\n    // the contract. Keep contract.hasComments unchanged so the name-cell dot and\n    // its tooltip continue to represent general contract comments only.\n    var hasAnyComments = contract.hasComments || ((_a = contract.monthHasComments) === null || _a === void 0 ? void 0 : _a.some(Boolean)) === true || ((_b = contract.monthCommentTooltips) === null || _b === void 0 ? void 0 : _b.some(tooltip => tooltip.trim().length > 0)) === true;\n    var tdNumber = document.createElement(\"td\");\n    tdNumber.className = \"pcf-cell-tree pcf-cell-number pcf-cell-tree-contract\";\n    // Column 1 of the connector: only draw the account-level vertical line when more\n    // subaccounts follow below; for the last subaccount the slot must stay blank or\n    // the line \"leaks\" past the end of the account block.\n    var line1 = document.createElement(\"span\");\n    line1.className = \"pcf-tree-spacer\".concat(isLastSubaccount ? \"\" : \" pcf-tree-v\");\n    tdNumber.appendChild(line1);\n    // Column 2: this contract's own T/L branch (see method JSDoc).\n    var line2 = document.createElement(\"span\");\n    var contractBranchClass = isLastContract ? \"pcf-tree-l\" : \"pcf-tree-t\";\n    line2.className = \"pcf-tree-spacer \".concat(contractBranchClass).concat(connectsFromSubaccountToggle ? \" pcf-tree-from-toggle\" : \"\");\n    tdNumber.appendChild(line2);\n    // Leaf spacer substitutes for the toggle button contracts don't have, keeping\n    // the number column's indentation consistent across all three levels.\n    var leafSpacer = document.createElement(\"span\");\n    leafSpacer.className = \"pcf-leaf-spacer\";\n    tdNumber.appendChild(leafSpacer);\n    tr.appendChild(tdNumber);\n    var tdName = document.createElement(\"td\");\n    tdName.className = \"pcf-cell-name pcf-cell-name-contract\";\n    tdName.title = contract.name;\n    var nameContainer = document.createElement(\"div\");\n    nameContainer.className = \"pcf-name-block\";\n    var nameLine = document.createElement(\"div\");\n    nameLine.className = \"pcf-name-line\";\n    var mainName = document.createElement(\"span\");\n    mainName.className = \"pcf-name-main\".concat(contract.isStandardContract ? \" pcf-contract-standard\" : \"\");\n    mainName.innerText = contract.name;\n    mainName.title = contract.name;\n    nameLine.appendChild(mainName);\n    // HasComments => red dot beside the contract name; hover/focus shows the\n    // floating tooltip (CommentTooltip text) — see createCommentDot for why this\n    // dot uses a body-mounted tooltip instead of the CSS one.\n    if (contract.hasComments) {\n      nameLine.appendChild(this.createCommentDot(\"pcf-name-dot\", contract.commentTooltip));\n    }\n    nameContainer.appendChild(nameLine);\n    if (contract.subLabel) {\n      var subLabelSpan = document.createElement(\"span\");\n      subLabelSpan.className = \"pcf-name-sub\";\n      // Extract tags like [SPV], [DEVCO] from subLabel\n      // The tag gets its own span (pcf-meta-tag-spv / pcf-meta-tag-devco) so CSS\n      // can colour DevCo vs SPV differently; the remainder renders as plain text.\n      var tagMatch = contract.subLabel.match(/\\[([^\\]]+)\\]/);\n      var tag = tagMatch ? tagMatch[1] : \"\";\n      var description = contract.subLabel.replace(/\\[\\w+\\]/, \"\").trim();\n      if (tag) {\n        var tagSpan = document.createElement(\"span\");\n        tagSpan.className = \"pcf-meta-tag pcf-meta-tag-\".concat(tag.toLowerCase());\n        tagSpan.innerText = \"[\".concat(tag, \"]\");\n        subLabelSpan.appendChild(tagSpan);\n        if (description) {\n          subLabelSpan.appendChild(document.createTextNode(\" \".concat(description)));\n        }\n      } else {\n        subLabelSpan.innerText = contract.subLabel;\n      }\n      nameContainer.appendChild(subLabelSpan);\n    }\n    tdName.appendChild(nameContainer);\n    tr.appendChild(tdName);\n    this.appendNumericCell(tr, contract.totalCost, \"pcf-cell-total pcf-col-cost\" + (this.isContractFullyPaid(contract) ? \" pcf-cell-total-paid\" : \"\"));\n    if (showPlanned) {\n      this.appendNumericCell(tr, contract.actualCost, \"pcf-cell-total pcf-col-cost\");\n      this.appendNumericCell(tr, contract.plannedCost, \"pcf-cell-total pcf-col-cost\");\n    }\n    contract.months.forEach((val, monthIndex) => this.appendContractMonthCell(tr, contract, val, monthIndex, startYear));\n    var tdActions = document.createElement(\"td\");\n    tdActions.className = \"pcf-cell-actions pcf-col-actions\";\n    var btnActions = document.createElement(\"button\");\n    btnActions.className = \"pcf-dots-btn\";\n    btnActions.innerHTML = \"&bull;&bull;&bull;\";\n    // Small badge on the three-dot button is shown for either a general comment\n    // or at least one monthly cost-cell comment.\n    if (hasAnyComments) {\n      var dot = document.createElement(\"span\");\n      dot.className = \"pcf-dot-badge\";\n      btnActions.appendChild(dot);\n    }\n    btnActions.onclick = e => {\n      e.stopPropagation();\n      // Only one popup may be open at a time — close our own dropdowns before\n      // delegating to ActionDropdown (which fires Edit/Delete/Comment actions).\n      this.closeAddDropdown();\n      this.closeMonthDropdown();\n      this._actionDropdown.show(e, contract.id, hasAnyComments);\n    };\n    tdActions.appendChild(btnActions);\n    tr.appendChild(tdActions);\n    return tr;\n  }\n  /**\n   * The \"Add\" pseudo-row appended after a subaccount's contracts. FRAGILE connectors:\n   * line1 mirrors the contract rows (account line passes through only when more\n   * subaccounts follow), but line2 is INTENTIONALLY a bare spacer with no branch\n   * class — the subaccount's contract connector must terminate at the last real\n   * contract; letting it continue into this Add row was a past regression.\n   */\n  createAddRow(subaccount, showPlanned, isLastSubaccount) {\n    var tr = document.createElement(\"tr\");\n    tr.className = \"pcf-row-add\";\n    var tdNumber = document.createElement(\"td\");\n    tdNumber.className = \"pcf-cell-tree pcf-cell-number pcf-cell-tree-add\";\n    var line1 = document.createElement(\"span\");\n    line1.className = \"pcf-tree-spacer\".concat(isLastSubaccount ? \"\" : \" pcf-tree-v\");\n    tdNumber.appendChild(line1);\n    // No pcf-tree-v / branch class on purpose — see method JSDoc.\n    var line2 = document.createElement(\"span\");\n    line2.className = \"pcf-tree-spacer\";\n    tdNumber.appendChild(line2);\n    var leafSpacer = document.createElement(\"span\");\n    leafSpacer.className = \"pcf-leaf-spacer\";\n    tdNumber.appendChild(leafSpacer);\n    tr.appendChild(tdNumber);\n    var tdContent = document.createElement(\"td\");\n    tdContent.className = \"pcf-cell-name pcf-cell-name-add\";\n    var btnAdd = document.createElement(\"button\");\n    btnAdd.className = \"pcf-add-btn pcf-add-menu-btn\";\n    btnAdd.innerHTML = \"<span class=\\\"pcf-add-menu-dots\\\">&bull;&bull;&bull;</span><span>Add</span>\";\n    btnAdd.onclick = e => {\n      e.stopPropagation();\n      this.showAddDropdown(e, subaccount);\n    };\n    tdContent.appendChild(btnAdd);\n    tr.appendChild(tdContent);\n    // Pad with empty cells (Total + optional Planned/Actual + 12 months + actions)\n    // instead of a colspan so the column borders stay continuous through this row.\n    var remainingCols = 1 + (showPlanned ? 2 : 0) + 12 + 1;\n    for (var i = 0; i < remainingCols; i++) {\n      var tdEmpty = document.createElement(\"td\");\n      tr.appendChild(tdEmpty);\n    }\n    return tr;\n  }\n  /**\n   * Footer row appended LAST into tbody (not tfoot) so syncGrandTotalSpacer can\n   * insert its filler row directly before it; CSS makes it sticky at the bottom of\n   * the scroll wrapper. Totals were accumulated in render() from the filtered tree.\n   */\n  renderGrandTotal(grandTotalCost, grandPlannedCost, grandActualCost, grandMonthlyTotals, showPlanned) {\n    var trGrand = document.createElement(\"tr\");\n    trGrand.className = \"pcf-row-grand\";\n    var tdGrandLabel1 = document.createElement(\"td\");\n    tdGrandLabel1.className = \"pcf-grand-label\";\n    tdGrandLabel1.innerText = \"Grand Total\";\n    tdGrandLabel1.setAttribute(\"colspan\", \"2\");\n    trGrand.appendChild(tdGrandLabel1);\n    this.appendNumericCell(trGrand, grandTotalCost, \"pcf-cell-total pcf-col-cost\");\n    if (showPlanned) {\n      this.appendNumericCell(trGrand, grandActualCost, \"pcf-cell-total pcf-col-cost\");\n      this.appendNumericCell(trGrand, grandPlannedCost, \"pcf-cell-total pcf-col-cost\");\n    }\n    grandMonthlyTotals.forEach(val => this.appendNumericCell(trGrand, val, \"pcf-cell-month\"));\n    var tdGrandActions = document.createElement(\"td\");\n    tdGrandActions.className = \"pcf-cell-actions\";\n    trGrand.appendChild(tdGrandActions);\n    this._tbody.appendChild(trGrand);\n  }\n  /** Thin visual breathing-space row between two expanded account blocks. */\n  createGroupGapRow(colSpan) {\n    var tr = document.createElement(\"tr\");\n    tr.className = \"pcf-row-gap\";\n    var td = document.createElement(\"td\");\n    td.colSpan = colSpan;\n    tr.appendChild(td);\n    return tr;\n  }\n  /**\n   * Plain (non-interactive) numeric cell used by account/subaccount/grand-total rows.\n   * Zero/null/NaN render as a styled \"-\" dash — business preference: an empty-looking\n   * grid is easier to scan than a wall of zeros.\n   */\n  /**\n   * Bug 13453: a CONTRACT row's Total Costs cell turns green when Total Cost\n   * equals Total Paid (actualCost) - i.e. the contract is fully paid. Compares\n   * the all-years totals (not the selected-year monthPaid flags) so a contract\n   * with costs across multiple years only greens when EVERY year is paid.\n   * Epsilon absorbs cent-level float noise so exact equality still matches.\n   */\n  isContractFullyPaid(contract) {\n    return contract.totalCost > 0 && Math.abs(contract.totalCost - contract.actualCost) < 0.005;\n  }\n  appendNumericCell(tr, val) {\n    var className = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : \"\";\n    var td = document.createElement(\"td\");\n    td.className = \"pcf-col-num \".concat(className).trim();\n    if (val === null || val === undefined || isNaN(val) || val === 0) {\n      td.innerText = \"-\";\n      td.classList.add(\"pcf-val-dash\");\n    } else {\n      td.innerText = this.formatCostValue(val);\n    }\n    tr.appendChild(td);\n  }\n  /**\n   * Interactive month cell on contract rows. Combines:\n   * - MxPaid flag => green (paid) / default (unpaid) background classes; paid cells\n   *   also get a native \"Paid Cost\" hover tooltip (Bug 13284);\n   * - MxHasComments / MxCommentTooltip => red dot + CSS data-tooltip;\n   * - click / Enter / Space => paid-unpaid dropdown (showMonthActionDropdown).\n   * Cells with no cost (\"-\") are intentionally NOT clickable — there is nothing to\n   * mark paid — but they still render a comment dot if Canvas flags one.\n   */\n  appendContractMonthCell(tr, contract, val, monthIndex, startYear) {\n    var _a, _b, _c;\n    var td = document.createElement(\"td\");\n    td.className = \"pcf-col-num pcf-cell-month pcf-contract-month-cell\";\n    var monthCommentTooltip = ((_a = contract.monthCommentTooltips) === null || _a === void 0 ? void 0 : _a[monthIndex]) || \"\";\n    // Tooltip text alone also counts as \"has comments\" — guards against Canvas\n    // sending MxCommentTooltip without setting the MxHasComments flag.\n    var hasMonthComments = ((_b = contract.monthHasComments) === null || _b === void 0 ? void 0 : _b[monthIndex]) || monthCommentTooltip.length > 0;\n    if (val === null || val === undefined || isNaN(val) || val === 0) {\n      this.setMonthCellContent(td, \"-\", hasMonthComments, monthCommentTooltip);\n      td.classList.add(\"pcf-val-dash\");\n      tr.appendChild(td);\n      return;\n    }\n    this.setMonthCellContent(td, this.formatCostValue(val), hasMonthComments, monthCommentTooltip);\n    var isPaid = ((_c = contract.monthPaid) === null || _c === void 0 ? void 0 : _c[monthIndex]) || false;\n    if (isPaid) {\n      td.classList.add(\"pcf-paid-cell\");\n      // Bug 13284: hovering a paid (green) cost cell shows a \"Paid Cost\" tooltip.\n      td.title = \"Paid Cost\";\n    } else {\n      td.classList.add(\"pcf-unpaid-cell\");\n    }\n    // Keyboard accessibility: the td acts as a button (tab focus + Enter/Space),\n    // required because Canvas users may operate the grid without a mouse.\n    td.tabIndex = 0;\n    td.setAttribute(\"role\", \"button\");\n    td.setAttribute(\"aria-label\", \"\".concat(this.formatCostValue(val), \". Click to \").concat(isPaid ? \"set as unpaid\" : \"set as paid\", \".\"));\n    td.onclick = e => {\n      e.stopPropagation();\n      this.showMonthActionDropdown(e, contract, monthIndex, startYear, isPaid, td);\n    };\n    td.onkeydown = e => {\n      if (e.key !== \"Enter\" && e.key !== \" \") {\n        return;\n      }\n      e.preventDefault();\n      e.stopPropagation();\n      this.showMonthActionDropdown(e, contract, monthIndex, startYear, isPaid, td);\n    };\n    tr.appendChild(td);\n  }\n  /**\n   * Writes a month cell's value + optional comment dot. The value gets its own span\n   * (pcf-month-value) so CSS can position the dot relative to the number without\n   * disturbing the cell's right-aligned layout.\n   */\n  setMonthCellContent(td, value, hasComments, commentTooltip) {\n    var valueSpan = document.createElement(\"span\");\n    valueSpan.className = \"pcf-month-value\";\n    valueSpan.innerText = value;\n    td.appendChild(valueSpan);\n    if (hasComments) {\n      td.appendChild(this.createCommentDot(\"pcf-month-comment-dot\", commentTooltip));\n    }\n  }\n  /**\n   * Red comment-indicator dot, in two tooltip flavours:\n   * - name-cell dots (\"pcf-name-dot\") use the JS floating tooltip on document.body\n   *   (see inline note below — the name cell clips overflow for its ellipsis);\n   * - month-cell dots use the cheaper pure-CSS data-tooltip, which is safe there\n   *   because month cells don't clip.\n   * The dot swallows clicks so hovering users don't accidentally open the\n   * paid/unpaid dropdown of the underlying month cell.\n   */\n  createCommentDot(className, commentTooltip) {\n    var dot = document.createElement(\"span\");\n    dot.className = \"\".concat(className, \" pcf-comment-dot\");\n    var tooltipText = this.formatCommentTooltip(commentTooltip);\n    if (tooltipText) {\n      dot.setAttribute(\"aria-label\", tooltipText);\n      dot.tabIndex = 0;\n      if (className === \"pcf-name-dot\") {\n        // The name cell clips overflow for the ellipsis, so a CSS tooltip would be\n        // cut off; float this one on document.body like the dropdowns.\n        dot.title = \"\";\n        dot.onmouseenter = () => this.showCommentTooltip(dot, tooltipText);\n        dot.onmouseleave = () => this.hideCommentTooltip();\n        dot.onfocus = () => this.showCommentTooltip(dot, tooltipText);\n        dot.onblur = () => this.hideCommentTooltip();\n      } else {\n        dot.setAttribute(\"data-tooltip\", tooltipText);\n      }\n    }\n    dot.onclick = e => e.stopPropagation();\n    return dot;\n  }\n  /**\n   * Dark floating tooltip for the contract-name comment dot. Mounted on\n   * document.body to escape the name cell's overflow clipping; positioned centered\n   * above the dot, clamped to the viewport, and FLIPPED BELOW when there is no room\n   * above. render() calls hideCommentTooltip so it never outlives its anchor.\n   */\n  showCommentTooltip(anchor, text) {\n    // Only one tooltip at a time — a stale one would never get a mouseleave.\n    this.hideCommentTooltip();\n    var tooltip = document.createElement(\"div\");\n    tooltip.className = \"pcf-comment-tooltip-floating\";\n    tooltip.innerText = text;\n    document.body.appendChild(tooltip);\n    // Measure AFTER appending (offsetWidth/Height are 0 before layout), then clamp\n    // horizontally and flip below the anchor when the space above is insufficient.\n    var rect = anchor.getBoundingClientRect();\n    var viewportPadding = 8;\n    var width = tooltip.offsetWidth;\n    var height = tooltip.offsetHeight;\n    var left = Math.max(viewportPadding, Math.min(rect.left + rect.width / 2 - width / 2, window.innerWidth - width - viewportPadding));\n    var top = rect.top - height - 8 >= viewportPadding ? rect.top - height - 8 : rect.bottom + 8;\n    tooltip.style.left = \"\".concat(left, \"px\");\n    tooltip.style.top = \"\".concat(top, \"px\");\n    this._commentTooltipElement = tooltip;\n  }\n  /** Removes the floating tooltip; called on mouseleave/blur AND on every render()\n   *  so a re-render can't strand a tooltip whose anchor no longer exists. */\n  hideCommentTooltip() {\n    var _a;\n    if ((_a = this._commentTooltipElement) === null || _a === void 0 ? void 0 : _a.parentNode) {\n      this._commentTooltipElement.parentNode.removeChild(this._commentTooltipElement);\n    }\n    this._commentTooltipElement = null;\n  }\n  /** Normalises tooltip text to always lead with \"Comment:\" without doubling the\n   *  prefix when Canvas already included it in CommentTooltip/MxCommentTooltip. */\n  formatCommentTooltip(commentTooltip) {\n    var text = (commentTooltip || \"\").trim();\n    if (!text) {\n      return \"\";\n    }\n    return text.toLowerCase().startsWith(\"comment:\") ? text : \"Comment:\\n\".concat(text);\n  }\n  /** Inline SVG (speech bubble) for the month dropdown's \"Comments\" item — inlined\n   *  rather than an asset so the PCF bundle stays self-contained. */\n  getCommentActionIcon() {\n    return \"\\n            <svg viewBox=\\\"0 0 24 24\\\" aria-hidden=\\\"true\\\" focusable=\\\"false\\\">\\n                <path d=\\\"M4.5 5.25h15a1.5 1.5 0 0 1 1.5 1.5v8.5a1.5 1.5 0 0 1-1.5 1.5H11.5l-4 3.5v-3.5h-3a1.5 1.5 0 0 1-1.5-1.5v-8.5a1.5 1.5 0 0 1 1.5-1.5z\\\"/>\\n                <path d=\\\"M7.25 9.25h9.5\\\"/>\\n                <path d=\\\"M7.25 12.75h6.5\\\"/>\\n            </svg>\";\n  }\n  /** Inline SVG (banknote) for the \"Set as paid/unpaid\" dropdown item. */\n  getPaidActionIcon() {\n    return \"\\n            <svg viewBox=\\\"0 0 24 24\\\" aria-hidden=\\\"true\\\" focusable=\\\"false\\\">\\n                <path d=\\\"M3.75 7.25h16.5v9.5H3.75z\\\"/>\\n                <path d=\\\"M6.75 7.25a3 3 0 0 1-3 3\\\"/>\\n                <path d=\\\"M20.25 10.25a3 3 0 0 1-3-3\\\"/>\\n                <path d=\\\"M3.75 13.75a3 3 0 0 1 3 3\\\"/>\\n                <path d=\\\"M17.25 16.75a3 3 0 0 1 3-3\\\"/>\\n                <circle cx=\\\"12\\\" cy=\\\"12\\\" r=\\\"2.35\\\"/>\\n            </svg>\";\n  }\n  /**\n   * Dropdown for the per-subaccount \"Add\" button. Item rules (business-agreed):\n   * - \"Add New Cost\" is always present (fires the \"Add\" action).\n   * - Standard contracts, depending on how many options this subaccount has:\n   *     >1  => \"Add Standard Contract\" parent item with a hover submenu, one entry\n   *            per option (each fires \"AddStandardContract\" with that option's id);\n   *     ==1 => a single direct item, no submenu (saves a click);\n   *     0   => NO standard item at all. An earlier design wanted a disabled item\n   *            here instead — intentionally NOT implemented, do not \"restore\" it.\n   * Appended to document.body so the scroll container can't clip it; flips above the\n   * button when there is no room below.\n   */\n  showAddDropdown(e, subaccount) {\n    var target = e.currentTarget;\n    // Toggle: re-clicking the button that owns the open menu closes it instead of\n    // reopening (the button's onclick stops propagation, so the window light-dismiss\n    // never fires for it — this is the sole toggle point).\n    if (this._addDropdownElement && this._activeAddMenuButton === target) {\n      this.closeAddDropdown();\n      return;\n    }\n    // Enforce the single-open-popup rule across all three menu types.\n    this.closeAddDropdown();\n    this.closeMonthDropdown();\n    this._actionDropdown.closeDropdown();\n    var rect = target.getBoundingClientRect();\n    var standardContracts = subaccount.standardContractOptions;\n    var dropdown = document.createElement(\"div\");\n    dropdown.className = \"pcf-add-dropdown open\";\n    var addNewCostButton = this.createAddDropdownButton(\"add\", \"Add New Cost\", false, () => {\n      this._onActionTriggered(\"Add\", subaccount.id);\n      this.closeAddDropdown();\n    });\n    dropdown.appendChild(addNewCostButton);\n    if (standardContracts.length > 1) {\n      var wrapper = document.createElement(\"div\");\n      wrapper.className = \"pcf-add-dropdown-item-wrap\";\n      var standardButton = this.createAddDropdownButton(\"standard\", \"Add Standard Contract\", false);\n      standardButton.classList.add(\"pcf-add-dropdown-parent\");\n      wrapper.appendChild(standardButton);\n      var subMenu = document.createElement(\"div\");\n      subMenu.className = \"pcf-add-submenu\";\n      standardContracts.forEach(option => {\n        subMenu.appendChild(this.createAddDropdownButton(\"standard\", option.name, false, () => {\n          this._onActionTriggered(\"AddStandardContract\", subaccount.id, option.id);\n          this.closeAddDropdown();\n        }));\n      });\n      wrapper.appendChild(subMenu);\n      dropdown.appendChild(wrapper);\n    } else if (standardContracts.length === 1) {\n      var option = standardContracts[0];\n      dropdown.appendChild(this.createAddDropdownButton(\"standard\", \"Add Standard Contract\", false, () => {\n        this._onActionTriggered(\"AddStandardContract\", subaccount.id, option.id);\n        this.closeAddDropdown();\n      }));\n    }\n    document.body.appendChild(dropdown);\n    this._addDropdownElement = dropdown;\n    // Blue \"active\" highlight on the Add button while its menu is open.\n    this._activeAddMenuButton = target;\n    target.classList.add(\"pcf-add-menu-btn-active\");\n    // Position after mounting (sizes are 0 until layout); prefer below the button,\n    // flip above when it would overflow the bottom of the viewport.\n    var menuWidth = dropdown.offsetWidth;\n    var menuHeight = dropdown.offsetHeight;\n    var viewportPadding = 8;\n    var left = Math.max(viewportPadding, Math.min(rect.left, window.innerWidth - menuWidth - viewportPadding));\n    var preferredTop = rect.bottom + 4;\n    var top = preferredTop + menuHeight <= window.innerHeight - viewportPadding ? preferredTop : Math.max(viewportPadding, rect.top - menuHeight - 4);\n    dropdown.style.left = \"\".concat(left, \"px\");\n    dropdown.style.top = \"\".concat(top, \"px\");\n    // Window-level listener implements light-dismiss (click anywhere else closes).\n    window.addEventListener(\"click\", this._boundAddDropdownWindowClick);\n  }\n  /**\n   * Shared factory for Add-dropdown items. `onClick` is optional because the submenu\n   * parent item is hover-only; the `disabled` flag is currently always false but kept\n   * for the (shelved) disabled-standard-item design mentioned in showAddDropdown.\n   */\n  createAddDropdownButton(icon, label, disabled, onClick) {\n    var button = document.createElement(\"button\");\n    button.className = \"pcf-add-dropdown-item\";\n    button.disabled = disabled;\n    button.innerHTML = \"<span class=\\\"pcf-add-dropdown-icon\\\">\".concat(this.getAddDropdownIcon(icon), \"</span><span>\").concat(label, \"</span>\");\n    if (onClick) {\n      button.onclick = e => {\n        e.stopPropagation();\n        onClick();\n      };\n    }\n    return button;\n  }\n  /** Inline SVGs for the Add dropdown: plus sign vs document-with-$ (standard contract). */\n  getAddDropdownIcon(icon) {\n    if (icon === \"add\") {\n      return \"\\n                <svg viewBox=\\\"0 0 20 20\\\" aria-hidden=\\\"true\\\">\\n                    <path d=\\\"M10 3.5a.5.5 0 0 1 .5.5v5.5H16a.5.5 0 0 1 0 1h-5.5V16a.5.5 0 0 1-1 0v-5.5H4a.5.5 0 0 1 0-1h5.5V4a.5.5 0 0 1 .5-.5z\\\"/>\\n                </svg>\";\n    }\n    return \"\\n            <svg viewBox=\\\"0 0 20 20\\\" aria-hidden=\\\"true\\\">\\n                <path d=\\\"M5.5 2A1.5 1.5 0 0 0 4 3.5v13A1.5 1.5 0 0 0 5.5 18h9a1.5 1.5 0 0 0 1.5-1.5V7.25a1.5 1.5 0 0 0-.44-1.06l-3.75-3.75A1.5 1.5 0 0 0 10.75 2h-5zM5 3.5a.5.5 0 0 1 .5-.5H10v3.5A1.5 1.5 0 0 0 11.5 8H15v8.5a.5.5 0 0 1-.5.5h-9a.5.5 0 0 1-.5-.5v-13zm6 0L14.5 7h-3a.5.5 0 0 1-.5-.5v-3z\\\"/>\\n                <path d=\\\"M8.25 9.25h3.25a.5.5 0 0 1 0 1H8.25a1.25 1.25 0 0 0 0 2.5h2.5a2.25 2.25 0 0 1 0 4.5H7.5a.5.5 0 0 1 0-1h3.25a1.25 1.25 0 0 0 0-2.5h-2.5a2.25 2.25 0 0 1 0-4.5z\\\"/>\\n            </svg>\";\n  }\n  /**\n   * Light-dismiss handler for the Add dropdown. Clicks on the Add button itself or\n   * inside the menu are ignored so the button's own onclick (which re-opens) and the\n   * submenu hover interactions don't fight with this close path.\n   */\n  handleAddDropdownWindowClick(e) {\n    var target = e.target;\n    if (target.closest(\".pcf-add-menu-btn\") || target.closest(\".pcf-add-dropdown\")) {\n      return;\n    }\n    this.closeAddDropdown();\n  }\n  /** Tears down the Add dropdown AND its window listener — both must go together or\n   *  the stale listener keeps firing on every click for the control's lifetime. */\n  closeAddDropdown() {\n    var _a;\n    window.removeEventListener(\"click\", this._boundAddDropdownWindowClick);\n    if ((_a = this._addDropdownElement) === null || _a === void 0 ? void 0 : _a.parentNode) {\n      this._addDropdownElement.parentNode.removeChild(this._addDropdownElement);\n    }\n    this._addDropdownElement = null;\n    // Drop the blue highlight from the Add button the menu was open for.\n    if (this._activeAddMenuButton) {\n      this._activeAddMenuButton.classList.remove(\"pcf-add-menu-btn-active\");\n      this._activeAddMenuButton = null;\n    }\n  }\n  /**\n   * Month-cell dropdown (\"Set as paid/unpaid\" + \"Comments\").\n   * - Cluster/equal-distributed contracts (isEqualDistribution): the paid toggle is\n   *   shown but DISABLED with an info icon + tooltip — business rule says such costs\n   *   cannot be marked paid per-month because their amounts are derived, not entered.\n   *   The disabled item still renders (instead of being hidden) so users learn WHY.\n   * - \"Comments\" appears only when the paid action is enabled and deliberately fires\n   *   the SAME \"Comment\" action as the contract row's three-dot menu — Canvas opens\n   *   one shared comment dialog for both entry points.\n   * Mounted on document.body for the same clipping reasons as the Add dropdown.\n   */\n  showMonthActionDropdown(e, contract, monthIndex, startYear, isPaid, td) {\n    // Toggle (Bug 13292): clicking the cell that already owns the open dropdown\n    // closes it instead of closing-then-reopening. Must run BEFORE closeMonthDropdown\n    // (which clears _activeMonthCell), and the window light-dismiss intentionally\n    // ignores month-cell clicks so this handler is the sole toggle point.\n    if (this._monthDropdownElement && this._activeMonthCell === td) {\n      this.closeMonthDropdown();\n      return;\n    }\n    this.closeAddDropdown();\n    this.closeMonthDropdown();\n    this._actionDropdown.closeDropdown();\n    // Highlight the clicked cell while its menu is open; closeMonthDropdown removes it.\n    this._activeMonthCell = td;\n    td.classList.add(\"active\");\n    var target = e.currentTarget;\n    var rect = target.getBoundingClientRect();\n    var dropdown = document.createElement(\"div\");\n    dropdown.className = \"pcf-month-dropdown open\";\n    var isCluster = this.isEqualDistribution(contract.distributionType);\n    var targetPaidState = !isPaid;\n    if (isCluster) {\n      // Finalized requirement (equal distribution by cluster OR by start/end\n      // date): month cells offer NO actions - these costs are machine-\n      // generated, so they can be neither marked paid nor given payment-date\n      // comments. Clicking a cell shows only this message; the \"Set as paid\"\n      // button and the Comments item are intentionally not rendered.\n      dropdown.classList.add(\"pcf-month-dropdown--info\");\n      var message = document.createElement(\"div\");\n      message.className = \"pcf-month-dropdown-message\";\n      message.innerText = \"Automatically distributed costs cannot be marked as paid or assigned payment date comments.\";\n      dropdown.appendChild(message);\n    } else {\n      var actionButton = document.createElement(\"button\");\n      actionButton.className = \"pcf-dropdown-item pcf-month-dropdown-item\";\n      actionButton.innerHTML = \"\\n                <span class=\\\"pcf-paid-action-icon\\\">\".concat(this.getPaidActionIcon(), \"</span>\\n                <span>\").concat(isPaid ? \"Set as unpaid\" : \"Set as paid\", \"</span>\\n            \");\n      actionButton.onclick = ev => {\n        ev.stopPropagation();\n        this.closeMonthDropdown();\n        // month is converted to 1-based here because Canvas expects\n        // SelectedCostMonth as the human month number (1..12).\n        this._onActionTriggered(\"SetCostPaidUnpaid\", contract.id, \"\", {\n          year: startYear,\n          month: monthIndex + 1,\n          paid: targetPaidState\n        });\n      };\n      dropdown.appendChild(actionButton);\n      var commentButton = document.createElement(\"button\");\n      commentButton.className = \"pcf-dropdown-item pcf-month-dropdown-item\";\n      commentButton.innerHTML = \"\\n                <span class=\\\"pcf-paid-action-icon\\\">\".concat(this.getCommentActionIcon(), \"</span>\\n                <span>Comments</span>\\n            \");\n      commentButton.onclick = ev => {\n        ev.stopPropagation();\n        this.closeMonthDropdown();\n        this._onActionTriggered(\"Comment\", contract.id);\n      };\n      dropdown.appendChild(commentButton);\n    }\n    document.body.appendChild(dropdown);\n    this._monthDropdownElement = dropdown;\n    var menuWidth = dropdown.offsetWidth;\n    var menuHeight = dropdown.offsetHeight;\n    var viewportPadding = 8;\n    var left = Math.max(viewportPadding, Math.min(rect.left + rect.width / 2 - menuWidth / 2, window.innerWidth - menuWidth - viewportPadding));\n    var preferredTop = rect.bottom + 4;\n    var top = preferredTop + menuHeight <= window.innerHeight - viewportPadding ? preferredTop : Math.max(viewportPadding, rect.top - menuHeight - 4);\n    dropdown.style.left = \"\".concat(left, \"px\");\n    dropdown.style.top = \"\".concat(top, \"px\");\n    window.addEventListener(\"click\", this._boundMonthDropdownWindowClick);\n  }\n  /**\n   * Detects cluster/equal-distribution contracts whose months must not be marked\n   * paid. DELIBERATELY broad: strips all punctuation/whitespace and substring-matches\n   * \"equal\"/\"cluster\"/\"distributed\" because the DistributionType text differs across\n   * environments and legacy rows (\"Equal Distribution\", \"Cluster-distributed\", ...).\n   * Tightening this to exact matches would silently re-enable the paid toggle for\n   * some legacy spellings.\n   */\n  isEqualDistribution(distributionType) {\n    var normalized = (distributionType || \"\").toLowerCase().replace(/[^a-z0-9]/g, \"\");\n    return normalized.includes(\"equal\") || normalized.includes(\"cluster\") || normalized.includes(\"distributed\");\n  }\n  /**\n   * Light-dismiss for the month dropdown. Clicks on month cells are excluded because\n   * the cell's own onclick already closes the old menu and opens the new one —\n   * closing here as well would cancel that reopen.\n   */\n  handleMonthDropdownWindowClick(e) {\n    var target = e.target;\n    if (target.closest(\".pcf-month-dropdown\") || target.closest(\".pcf-contract-month-cell\")) {\n      return;\n    }\n    this.closeMonthDropdown();\n  }\n  /** Tears down the month dropdown, its window listener, and the active-cell\n   *  highlight — all three must be cleaned together to avoid stale UI state. */\n  closeMonthDropdown() {\n    var _a;\n    window.removeEventListener(\"click\", this._boundMonthDropdownWindowClick);\n    if ((_a = this._monthDropdownElement) === null || _a === void 0 ? void 0 : _a.parentNode) {\n      this._monthDropdownElement.parentNode.removeChild(this._monthDropdownElement);\n    }\n    this._monthDropdownElement = null;\n    if (this._activeMonthCell) {\n      this._activeMonthCell.classList.remove(\"active\");\n      this._activeMonthCell = null;\n    }\n  }\n  /**\n   * Formats costs with the BROWSER's locale, not a hard-coded one, so English users\n   * see \"1,000\" and German users see \"1.000\" — an explicit requirement for this\n   * mixed-locale customer. navigator.languages is preferred (full preference list);\n   * single navigator.language is the fallback for older WebViews.\n   */\n  formatCostValue(value) {\n    var browserLocales = navigator.languages && navigator.languages.length > 0 ? navigator.languages : [navigator.language];\n    return new Intl.NumberFormat(browserLocales, {\n      minimumFractionDigits: 0,\n      maximumFractionDigits: 3\n    }).format(value);\n  }\n  /**\n   * Expand/collapse for account and subaccount rows. Mutates the SHARED collapse set\n   * (owned by index.ts) and then asks index.ts to re-render via the PCF_RE_RENDER\n   * custom event rather than calling render() directly — the renderer has no access\n   * to the latest PCF context, and letting index.ts orchestrate keeps a single render\n   * path for both host updates and internal UI state changes.\n   */\n  toggleCollapse(id) {\n    // Set holds EXPANDED ids: present => currently expanded, so collapse it;\n    // absent => currently collapsed, so expand it.\n    if (this._expandedRowIds.has(id)) {\n      this._expandedRowIds.delete(id);\n    } else {\n      this._expandedRowIds.add(id);\n    }\n    // Dispatching a custom event lets the main class orchestrate the re-render safely\n    var event = new CustomEvent(\"PCF_RE_RENDER\");\n    window.dispatchEvent(event);\n  }\n  /** Placeholder while the Canvas dataset is still loading; clears the header too so\n   *  stale column captions from a previous render don't sit above the message. */\n  renderLoading() {\n    this._thead.innerHTML = \"\";\n    this._tbody.innerHTML = \"\\n            <tr>\\n                <td colspan=\\\"100%\\\">\\n                    <div style=\\\"text-align:center; padding: 40px; color: #9ca3af; font-style:italic;\\\">\\n                        Loading project budget...\\n                    </div>\\n                </td>\\n            </tr>\\n        \";\n  }\n  /** Shown only when even the test fallback yields nothing (or filters removed\n   *  everything) — points the maker at the missing collection binding. */\n  renderEmptyState(colSpan) {\n    this._tbody.innerHTML = \"\\n            <tr>\\n                <td colspan=\\\"\".concat(colSpan, \"\\\">\\n                    <div class=\\\"pcf-loading-state\\\">\\n                        <div class=\\\"pcf-spinner\\\" role=\\\"status\\\" aria-label=\\\"Loading data\\\"></div>\\n                        <div>Loading data\\u2026</div>\\n                    </div>\\n                </td>\\n            </tr>\\n        \");\n  }\n  /**\n   * Called from index.ts destroy(). Critical because the dropdowns live on\n   * document.body and their dismiss listeners hang off window — without this they\n   * would outlive the control when the user navigates away from the Canvas screen.\n   */\n  destroy() {\n    this.closeAddDropdown();\n    this.closeMonthDropdown();\n    if (this._resizeObserver) {\n      this._resizeObserver.disconnect();\n      this._resizeObserver = null;\n    }\n  }\n}\n// Column min-widths (px), hand-tuned with renderColGroup(). By design the table is\n// allowed to grow BEYOND the Canvas-allocated width and scroll horizontally rather\n// than squeezing columns — shrinking these values causes wrapped numbers and broken\n// tree connector alignment, so change them only with a designer sign-off.\n// Number column also hosts the tree toggles + connector spans, so it needs slack.\nGridRenderer.NUMBER_COLUMN_MIN_WIDTH = 100;\n// Deliberately raised to 300 so contract names get room before the ellipsis kicks in.\nGridRenderer.ACCOUNT_NAME_COLUMN_MIN_WIDTH = 300;\nGridRenderer.TOTAL_COST_COLUMN_MIN_WIDTH = 100;\n// Planned/Actual are slightly wider because their header captions are longer.\nGridRenderer.PLANNED_COST_COLUMN_MIN_WIDTH = 120;\nGridRenderer.ACTUAL_COST_COLUMN_MIN_WIDTH = 120;\n// 65px is the floor at which a 6-digit monthly cost still fits without wrapping.\nGridRenderer.MONTH_COLUMN_MIN_WIDTH = 65;\n// Fixed (not minimum) — the three-dot button never needs to flex.\nGridRenderer.ACTION_COLUMN_WIDTH = 40;\n// Month-column header captions (Bug 13304): three-letter abbreviations, no year.\n// Index 0 = January; the header loop is 1-based so it reads [i - 1]. English/fixed\n// on purpose — the grid's other captions (\"Total Costs\" etc.) are also fixed English.\nGridRenderer.MONTH_ABBREVIATIONS = [\"Jan\", \"Feb\", \"Mar\", \"Apr\", \"May\", \"Jun\", \"Jul\", \"Aug\", \"Sep\", \"Oct\", \"Nov\", \"Dec\"];\n\n//# sourceURL=webpack://pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad/./TableConnectedToDataversePCF/ui/GridRenderer.ts?\n}");

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		if (!(moduleId in __webpack_modules__)) {
/******/ 			delete __webpack_module_cache__[moduleId];
/******/ 			var e = new Error("Cannot find module '" + moduleId + "'");
/******/ 			e.code = 'MODULE_NOT_FOUND';
/******/ 			throw e;
/******/ 		}
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = __webpack_require__("./TableConnectedToDataversePCF/index.ts");
/******/ 	pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad = __webpack_exports__;
/******/ 	
/******/ })()
;
if (window.ComponentFramework && window.ComponentFramework.registerControl) {
	ComponentFramework.registerControl('Dev.DevexCapexSummaryPCF', pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad.DevexCapexSummaryPCF);
} else {
	var Dev = Dev || {};
	Dev.DevexCapexSummaryPCF = pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad.DevexCapexSummaryPCF;
	pcf_tools_652ac3f36e1e4bca82eb3c1dc44e6fad = undefined;
}